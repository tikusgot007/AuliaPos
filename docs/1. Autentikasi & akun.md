| Nama Subbab        | Login                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan mekanisme autentikasi pengguna melalui username dan password, pembentukan session setelah login berhasil, serta penggunaan session sebagai dasar pembatasan akses aplikasi.                                                                                                                                                                                                                                                                                                             |
| Deskripsi          | AuliaPos menyediakan `GET /login` untuk menampilkan halaman login dan `POST /auth/proses-login` untuk memproses kredensial. `Auth::prosesLogin()` mengambil username dan password, mencari user melalui `UserModel`, kemudian memverifikasi password terhadap `password_hash`. Jika berhasil, session diisi dengan `id_user`, `username`, `inisial`, `role`, `isLoggedIn`, dan `last_activity`. Setelah autentikasi berhasil, admin diarahkan ke `/laporan`, sedangkan kasir diarahkan ke `/kasir`. |
| Aturan Bisnis      | Username dan password wajib diisi. Username harus unik pada tabel `users`. Password disimpan sebagai hash pada `password_hash` dan diverifikasi saat login. Role yang tersedia adalah `admin` dan `kasir`. `is_active` belum digunakan sebagai authentication gate pada proses login di branch `v2.1`.                                                                                                                                                                                              |
| Alur               | User membuka `/login` → mengisi username dan password → submit ke `/auth/proses-login` → `Auth::prosesLogin()` mencari user → password diverifikasi → session authentication dibuat → redirect berdasarkan role. Jika username tidak ditemukan atau password salah, login gagal dan user dikembalikan ke halaman login dengan flash error. Request berikutnya diperiksa oleh `AuthFilter`.                                                                                                          |
| Hak Akses          | `/login` dan `/auth/proses-login` tidak membutuhkan autentikasi karena digunakan sebelum session terbentuk. Route aplikasi yang menggunakan `auth` filter membutuhkan session login yang valid. Setelah login, `AuthFilter` menerapkan pembatasan route admin-only; user non-admin yang mengakses route tersebut diarahkan ke `/kasir`.                                                                                                                                                             |
| Kondisi & Batasan  | Session timeout setelah 30 menit berdasarkan `last_activity`. Form login menyediakan `csrf_field()`, tetapi global CSRF filter pada `Config/Filters.php` saat ini tidak diaktifkan. Pada source yang diaudit tidak ditemukan rate limiting, MFA, atau account lockout. `is_active = 0` belum memblokir proses login.                                                                                                                                                                                |
| Data yang Terlibat | Tabel `users`: `id`, `username`, `password_hash`, `role`, `is_active`. Session: `id_user`, `username`, `inisial`, `role`, `isLoggedIn`, `last_activity`. Komponen utama: `Auth.php`, `UserModel.php`, `Routes.php`, `AuthFilter.php`, `Filters.php`, dan `auth/login.php`.                                                                                                                                                                                                                          |
| Catatan            | Endpoint login sengaja dikecualikan dari `auth` filter agar user yang belum login dapat mengakses form dan mengirim kredensial. `is_active` perlu dibedakan dari authentication status karena pada `docs/USER-SHIFT.md` field tersebut digunakan untuk eligibility scheduling/Effective Shift Leader. `docs/AULIA.md` menjadi dokumentasi aktif, sedangkan `docs/archive/` digunakan sebagai referensi historis.                                                                                    |
| Status             | **Aktif — mekanisme login, session authentication, dan role-based access control terverifikasi pada branch `v2.1`; `is_active` belum menjadi authentication gate dan CSRF global masih menjadi hardening gap.**                                                                                                                                                                                                                                                                                     |

| Nama Subbab        | Logout                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menjelaskan mekanisme penghentian sesi autentikasi pengguna, penghapusan session setelah logout, serta behavior aplikasi ketika user kembali mengakses halaman yang membutuhkan autentikasi.                                                                                                                                                                                                                                                     |
| Deskripsi          | AuliaPos menyediakan `GET /logout` yang diarahkan ke `Auth::logout()`. Method tersebut menjalankan `session()->destroy()` untuk menghancurkan session pengguna, kemudian melakukan redirect ke `/login` dengan flash message `Anda berhasil logout.`. Tombol Logout tersedia pada dropdown profile di `app/Views/layout/main.php`.                                                                                                               |
| Aturan Bisnis      | Logout mengakhiri session authentication pengguna yang sedang aktif. Logout tidak menghapus atau mengubah data akun pada tabel `users`. Setelah session dihancurkan, user harus melakukan login kembali untuk memperoleh akses ke route yang dilindungi authentication filter.                                                                                                                                                                   |
| Alur               | User memilih **Logout** → browser mengakses `GET /logout` → `Auth::logout()` dipanggil → `session()->destroy()` menghancurkan session → sistem redirect ke `/login` dengan pesan berhasil logout → ketika user mengakses route yang memiliki `auth` filter, `AuthFilter` tidak menemukan `isLoggedIn` dan mengarahkan user kembali ke `/login`.                                                                                                  |
| Hak Akses          | Route `/logout` tidak menggunakan `auth` filter sehingga dapat dipanggil ketika session masih aktif maupun ketika session sudah tidak tersedia. Endpoint tersebut hanya menjalankan penghancuran session dan redirect; tidak memberikan akses terhadap resource aplikasi.                                                                                                                                                                        |
| Kondisi & Batasan  | Logout menggunakan `session()->destroy()` sehingga seluruh session dihancurkan, bukan hanya mengubah `isLoggedIn`. Endpoint logout menggunakan metode `GET`. Tidak terdapat parameter `id_user` atau target akun pada proses logout karena operasi dilakukan terhadap session pengguna yang sedang berjalan. Selain explicit logout, session juga dapat dihancurkan otomatis oleh `AuthFilter` ketika `last_activity` melewati timeout 30 menit. |
| Data yang Terlibat | Session authentication yang sebelumnya dibuat saat login: `id_user`, `username`, `inisial`, `role`, `isLoggedIn`, dan `last_activity`. Tidak ada perubahan pada tabel `users`. Komponen utama: `app/Controllers/Auth.php`, `app/Config/Routes.php`, `app/Filters/AuthFilter.php`, dan `app/Views/layout/main.php`.                                                                                                                               |
| Catatan            | `Auth::logout()` merupakan titik utama explicit logout. `AuthFilter` memiliki mekanisme penghentian session kedua melalui `session()->destroy()` ketika session timeout. Setelah logout maupun timeout, `/login` menjadi entry point untuk membentuk session authentication baru. Dokumentasi aktif `docs/AULIA.md` tidak menunjukkan business rule tambahan yang mengubah behavior logout tersebut.                                             |
| Status             | **Aktif — mekanisme logout terverifikasi pada branch `v2.1`; session dihancurkan melalui `session()->destroy()` dan user diarahkan kembali ke `/login`.**                                                                                                                                                                                                                                                                                        |

| Nama Subbab        | Ganti Password                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Memungkinkan pengguna yang sudah login mengganti password akunnya dengan terlebih dahulu membuktikan penguasaan password lama.                                                                                                                                                                                                                                                                                    |
| Deskripsi          | Fitur tersedia melalui `GET /ganti-password` dan `POST /auth/proses-ganti-password`. `Auth::prosesGantiPassword()` mengambil `id_user` dari session, memvalidasi password lama, password baru, dan konfirmasi, kemudian memperbarui `password_hash` melalui `UserModel::updatePassword()`. Setelah berhasil, pengguna diarahkan ke `/logout` sehingga harus login kembali menggunakan password baru.              |
| Aturan Bisnis      | Semua field wajib diisi. Password baru dan konfirmasi harus sama. Password baru minimal 6 karakter. Password lama harus berhasil diverifikasi terhadap `password_hash` yang tersimpan. Perubahan password dilakukan untuk user berdasarkan `session()->get('id_user')`, bukan berdasarkan ID user dari request.                                                                                                   |
| Alur               | User membuka `/ganti-password` → mengisi password lama, password baru, dan konfirmasi → submit ke `/auth/proses-ganti-password` → validasi input → user dicari berdasarkan `id_user` session → password lama diverifikasi → password baru di-hash dan disimpan → redirect ke `/logout` → session dihancurkan → user login ulang. Jika validasi gagal, user dikembalikan ke halaman sebelumnya dengan pesan error. |
| Hak Akses          | Hanya user yang memiliki session login valid. Kedua route menggunakan `auth` filter. Tidak ada pembatasan berdasarkan role; admin dan kasir dapat mengganti password akunnya sendiri.                                                                                                                                                                                                                             |
| Kondisi & Batasan  | Password baru hanya memiliki batas minimum 6 karakter; pada source yang diaudit tidak ditemukan aturan kompleksitas tambahan. Jika user sudah tidak login, proses diarahkan ke `/login`. Jika akun berdasarkan `id_user` session tidak ditemukan, proses ditolak. Setelah password berhasil diubah, session tidak dipertahankan sehingga login ulang diwajibkan.                                                  |
| Data yang Terlibat | Session `id_user`; tabel `users`, terutama `id` dan `password_hash`; input `password_lama`, `password_baru`, dan `konfirmasi_password`. Komponen utama: `Auth.php`, `UserModel.php`, `Routes.php`, `AuthFilter.php`, dan `auth/ganti_password.php`.                                                                                                                                                               |
| Catatan            | Proses tidak mengubah username, role, profile, atau data user lainnya. Redirect ke `/logout` digunakan sebagai mekanisme untuk memastikan session lama dihancurkan setelah credential berubah.                                                                                                                                                                                                                    |
| Status             | **Aktif — mekanisme ganti password terimplementasi dan route dilindungi `auth` filter pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                       |

| Nama Subbab        | Manajemen User                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menyediakan administrasi akun pengguna oleh admin, meliputi pembuatan, perubahan, penghapusan, status aktif, role, data profil, password, dan Priority.                                                                                                                                                                                                                                                                    |
| Deskripsi          | `Auth::userManagement()` menampilkan daftar user. Admin dapat menambah user melalui `tambahUser()`/`simpanUser()`, mengedit melalui `editUser()`/`updateUser()`, dan menghapus melalui `hapusUser()`. Data yang dikelola mencakup username, password, nama, inisial, divisi, nomor HP, role, status aktif, foto profil, dan Priority.                                                                                      |
| Aturan Bisnis      | Hanya role `admin` yang dapat mengakses Manajemen User. Username wajib diisi dan unik. Saat membuat user, password minimal 6 karakter dan role wajib dipilih. Role aplikasi adalah `admin` atau `kasir`. Priority bersifat opsional, harus berupa angka 0–65535, dan harus unik. Admin tidak boleh mengubah akun sendiri menjadi non-admin atau menonaktifkan akun sendiri. Admin juga tidak dapat menghapus akun sendiri. |
| Alur               | Admin membuka `/user-management` → memilih tambah/edit/hapus → backend memvalidasi session dan role → data divalidasi → untuk tambah/update password dilakukan hashing → data user disimpan/diperbarui/dihapus → jika operasi foto terlibat, `FotoProfilService` menangani file → redirect kembali ke Manajemen User dengan status operasi.                                                                                |
| Hak Akses          | Admin memiliki hak CRUD terhadap akun user lain. Kasir tidak memiliki akses Manajemen User. Enforcement dilakukan di backend melalui pengecekan `session()->get('role') === 'admin'` dan `AuthFilter` untuk route admin-only.                                                                                                                                                                                              |
| Kondisi & Batasan  | `is_active` dapat diubah oleh admin, tetapi pada implementasi login saat ini `is_active = 0` belum menjadi authentication gate. Username tidak boleh duplikat. Penghapusan diri sendiri dilarang. Perubahan role menjadi non-admin dan deaktivasi diri sendiri juga dilarang. Priority kosong selalu ditulis sebagai `NULL`, termasuk saat edit; tidak ada mode “pertahankan Priority lama”.                               |
| Data yang Terlibat | Tabel `users`: `id`, `username`, `password_hash`, `nama`, `inisial`, `divisi`, `no_hp`, `profile_photo`, `role`, `is_active`, `priority`, `created_at`. Komponen utama: `Auth.php`, `UserModel.php`, `Routes.php`, `AuthFilter.php`, view `user_management.php`, `tambah_user.php`, `edit_user.php`, dan `FotoProfilService`.                                                                                              |
| Catatan            | `docs/AULIA.md` menetapkan Manajemen User sebagai area admin untuk mengelola field akun, termasuk Priority. Dokumentasi juga menegaskan bahwa UI bukan enforcement; validasi backend tetap menjadi otoritas. Priority mempunyai aturan lintas-modul tersendiri di `docs/USER-SHIFT.md`.                                                                                                                                    |
| Status             | **Aktif — CRUD Manajemen User, role, status aktif, foto, password, dan Priority terimplementasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                      |

| Nama Subbab        | Profil Pengguna                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Memungkinkan setiap pengguna melihat dan memperbarui data profil miliknya sendiri tanpa memberikan akses untuk mengubah profil pengguna lain.                                                                                                                                                                                                                                              |
| Deskripsi          | Modul `Profil` menyediakan halaman `/profil` untuk user yang sudah login. Data yang dapat diubah sendiri adalah `nama` dan `no_hp`; foto profil ditangani melalui endpoint foto terpisah. `username`, `inisial`, `divisi`, `role`, dan `is_active` bersifat read-only pada Profil Saya. Controller selalu mengambil target dari `session()->get('id_user')`.                               |
| Aturan Bisnis      | Profil hanya berlaku untuk akun yang sedang login. User tidak mengirim `user_id` sebagai target perubahan. `Profil::index()`, `update()`, `uploadFoto()`, dan `hapusFoto()` menggunakan `id_user` dari session. `UserModel::updateProfilSaya()` membatasi field yang dapat diubah menjadi `nama` dan `no_hp`.                                                                              |
| Alur               | User membuka `/profil` → controller mengambil `id_user` dari session → mencari record user → menampilkan profil → user mengubah data yang diperbolehkan → submit `/profil/update` → backend kembali menentukan target dari session → `updateProfilSaya()` menyimpan perubahan → redirect kembali ke profil. Jika user tidak ditemukan, session dihancurkan dan user diarahkan ke `/login`. |
| Hak Akses          | Semua role yang sudah login dapat mengakses Profil Saya. Kasir dan admin dapat mengubah profil mereka sendiri, tetapi tidak dapat menggunakan modul ini untuk mengubah akun user lain.                                                                                                                                                                                                     |
| Kondisi & Batasan  | Field `username`, `inisial`, `divisi`, `role`, dan `is_active` tidak dapat diedit melalui Profil Saya. Perubahan data profil dibatasi oleh method khusus `updateProfilSaya()`, bukan update model bebas. Jika record user hilang ketika session masih aktif, sistem melakukan logout paksa.                                                                                                |
| Data yang Terlibat | Session `id_user`; tabel `users`, terutama `nama`, `no_hp`, `username`, `inisial`, `divisi`, `role`, `is_active`, dan `profile_photo`. Komponen utama: `Profil.php`, `UserModel.php`, route `/profil*`, dan view `profil/index.php`.                                                                                                                                                       |
| Catatan            | `docs/AULIA.md` secara eksplisit menetapkan pemisahan antara **Profil Saya** dan **Manajemen User**. Profil menggunakan ownership berbasis session, sedangkan pengelolaan user lain merupakan wewenang admin melalui `Auth`.                                                                                                                                                               |
| Status             | **Aktif — Profil Saya tersedia untuk semua role login dengan pembatasan ownership berbasis session.**                                                                                                                                                                                                                                                                                      |

| Nama Subbab        | Foto Profil                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menyediakan mekanisme upload, penggantian, dan penghapusan foto profil pengguna dengan validasi file dan pembatasan akses berdasarkan kepemilikan akun atau kewenangan admin.                                                                                                                                                                                                    |
| Deskripsi          | Foto disimpan sebagai nama file pada kolom `users.profile_photo`. Untuk Profil Saya, `Profil::uploadFoto()` dan `hapusFoto()` selalu menggunakan `session()->get('id_user')` sebagai target. Untuk Manajemen User, admin dapat mengelola foto user lain melalui `Auth::simpanUser()` dan `Auth::updateUser()`. Operasi file dipusatkan pada `FotoProfilService`.                 |
| Aturan Bisnis      | Foto bersifat opsional. Upload menggunakan file yang divalidasi sebagai gambar. Nama file dibuat oleh server, bukan menggunakan nama file asli user. Saat mengganti foto user lain melalui Manajemen User, file lama baru dihapus setelah update database berhasil. Jika operasi database gagal, file baru dibersihkan agar tidak menjadi orphan file.                           |
| Alur               | **Profil sendiri:** `/profil` → pilih foto → upload → `Profil::uploadFoto()` → `FotoProfilService::simpan()` → simpan filename ke `users.profile_photo`. **Admin:** tambah/edit user → upload foto opsional → service menyimpan file → filename disimpan bersama data user. Untuk penghapusan, field `profile_photo` dikosongkan lalu file lama dihapus setelah update berhasil. |
| Hak Akses          | Semua user login dapat mengelola foto profil sendiri. Admin dapat mengelola foto user lain melalui Manajemen User. Target Profil Saya tidak dapat ditentukan dari request karena selalu berasal dari session.                                                                                                                                                                    |
| Kondisi & Batasan  | Validasi upload pada `FotoProfilService` menggunakan batas ukuran maksimal 2 MB, whitelist MIME `jpeg/png/webp`, serta `getimagesize()` untuk memastikan file benar-benar gambar. File disimpan dengan nama server-generated. `profile_photo` hanya menyimpan nama file, bukan path lengkap atau binary.                                                                         |
| Data yang Terlibat | Tabel `users.profile_photo`; file gambar pada storage aplikasi; session `id_user`; `FotoProfilService`; `Profil.php`; `Auth.php`; view `profil/index.php`, `tambah_user.php`, dan `edit_user.php`.                                                                                                                                                                               |
| Catatan            | Dokumentasi aktif menetapkan bahwa foto profil merupakan bagian dari data user, bukan tabel `profile` terpisah. Dokumentasi historis juga mencatat bahwa endpoint tampilan foto melakukan streaming file dari disk dan nama file divalidasi sebelum path di-resolve.                                                                                                             |
| Status             | **Aktif — upload, penggantian, penghapusan, dan penyimpanan referensi foto profil terimplementasi pada branch `v2.1`.**                                                                                                                                                                                                                                                          |

| Nama Subbab        | Role & Hak Akses                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menentukan role dasar akun dan menerapkan pembatasan akses berdasarkan role pada route serta operasi aplikasi.                                                                                                                                                                                                                                                                                                                    |
| Deskripsi          | AuliaPos menggunakan dua role permanen pada `users.role`, yaitu `admin` dan `kasir`. Role disimpan pada database dan dimasukkan ke session ketika login. `AuthFilter` menggunakan session `role` untuk menerapkan admin-only route. Selain route gating, controller tertentu melakukan validasi role langsung sebelum menjalankan operasi sensitif.                                                                               |
| Aturan Bisnis      | `admin` dan `kasir` adalah role yang tersedia. Admin memiliki akses terhadap area administrasi seperti laporan, Manajemen User, Jadwal, Produk, Kategori, dan beberapa operasi administrasi lainnya. Kasir diarahkan ke area operasional dan ditolak ketika mengakses route admin-only. Priority tidak mengganti role menjadi `shift_leader`; Effective Shift Leader merupakan authority dinamis yang terpisah dari `users.role`. |
| Alur               | Login berhasil → `role` user dimasukkan ke session → request berikutnya masuk ke route yang memiliki `auth` filter → `AuthFilter` memeriksa `isLoggedIn`, timeout, dan role terhadap daftar admin route → jika non-admin mengakses route admin-only, request diarahkan ke `/kasir` → operasi yang memiliki pemeriksaan role tambahan kembali memvalidasi role di controller/backend.                                              |
| Hak Akses          | **Admin:** akses fitur administrasi yang masuk daftar admin route dan pengelolaan user. **Kasir:** akses fitur operasional yang tersedia untuk kasir dan tidak termasuk admin-only. **Effective Shift Leader:** bukan role database baru; authority ini dihitung berdasarkan Priority + jadwal + jendela kerja dan hanya memberikan kapabilitas tertentu yang didefinisikan eksplisit.                                            |
| Kondisi & Batasan  | Auth bersifat session-based. `AuthFilter` memiliki idle timeout 30 menit. Role tidak boleh dianggap sebagai satu-satunya konsep authority karena Effective Shift Leader menggunakan mekanisme terpisah. `docs/AULIA.md` menegaskan UI bukan enforcement; backend harus tetap memvalidasi role, ownership, dan kondisi bisnis.                                                                                                     |
| Data yang Terlibat | `users.role`; session `role`, `isLoggedIn`, `id_user`, dan `last_activity`; `AuthFilter::$adminRoutes`; `Routes.php`; controller yang melakukan pengecekan role; `Authority` dan `EffectiveShiftLeaderService` untuk authority dinamis.                                                                                                                                                                                           |
| Catatan            | `users.role` tetap hanya `admin`/`kasir`. `docs/USER-SHIFT.md` secara eksplisit menyatakan bahwa Priority tidak pernah ditulis menjadi role `shift_leader`. Effective Shift Leader juga tidak mengubah session role pengguna.                                                                                                                                                                                                     |
| Status             | **Aktif — role `admin`/`kasir`, route gating, dan authority dinamis Effective Shift Leader terimplementasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                  |

| Nama Subbab        | Priority Karyawan                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menyimpan ranking prioritas permanen karyawan yang menjadi salah satu input untuk menentukan Effective Shift Leader secara dinamis.                                                                                                                                                                                                                                                                                                              |
| Deskripsi          | Priority disimpan pada `users.priority` sebagai atribut permanen karyawan. Nilai lebih besar berarti prioritas lebih tinggi. Priority dikelola melalui Manajemen User oleh admin dan tidak melekat pada shift tertentu. Effective Shift Leader dihitung on-demand dari Priority, status aktif, role, jadwal, dan jendela kerja saat ini.                                                                                                         |
| Aturan Bisnis      | Priority bersifat opsional dan unik antar-karyawan. Nilai yang valid adalah bilangan bulat 0–65535. `NULL` berarti belum memiliki ranking, bukan Priority 0. Saat field dikosongkan pada create maupun edit, nilainya selalu menjadi `NULL`. Priority tidak mengubah `users.role`. Priority sendiri tidak memberikan hak akses; nilainya hanya menjadi input seleksi Effective Shift Leader.                                                     |
| Alur               | Admin membuka Manajemen User → mengisi/mengubah Priority → controller melakukan validasi format dan pengecekan duplikasi → database `UNIQUE KEY` menjadi otoritas akhir terhadap keunikan → Priority tersimpan di `users` → ketika authority dibutuhkan, `EffectiveShiftLeaderService` membaca kandidat berdasarkan jadwal dan status saat ini → kandidat dengan Priority tertinggi yang memenuhi seluruh syarat menjadi Effective Shift Leader. |
| Hak Akses          | Hanya admin yang dapat mengatur Priority melalui `/user-management`. Effective Shift Leader yang terbentuk dari Priority mendapatkan hanya kapabilitas yang secara eksplisit diberikan oleh sistem; Priority tidak memberikan akses administratif terhadap Manajemen User atau modul lain secara otomatis.                                                                                                                                       |
| Kondisi & Batasan  | Priority harus unik. Terdapat pre-check PHP untuk pesan error yang jelas dan unique constraint database untuk menangani race condition. Karyawan dengan Priority tinggi tetapi `is_active = 0`, tidak memiliki jadwal kerja yang sesuai, atau berada di luar jendela kerja tidak menjadi Effective Shift Leader. Admin tidak menjadi kandidat Effective Shift Leader karena kandidat harus memiliki `role = 'kasir'`.                            |
| Data yang Terlibat | `users.priority`, `users.role`, `users.is_active`; tabel `jadwal`; `EffectiveShiftLeaderService`; `Authority`; `JadwalModel::DEFINISI_SHIFT`; serta input Priority dari `Auth::simpanUser()` dan `Auth::updateUser()`.                                                                                                                                                                                                                           |
| Catatan            | `docs/USER-SHIFT.md` adalah single source of truth untuk Priority, Shift Member, dan Effective Shift Leader. Effective Shift Leader dihitung **satu global leader**, bukan satu leader per kode shift. Tidak ada state leader yang disimpan atau cache; perubahan Priority dapat memengaruhi hasil perhitungan request berikutnya.                                                                                                               |
| Status             | **Aktif — Priority, uniqueness constraint, pengelolaan melalui Manajemen User, dan integrasi dengan Effective Shift Leader terimplementasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                 |
