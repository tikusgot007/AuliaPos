| Nama Subbab        | Arsitektur Aplikasi                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan pola arsitektur AuliaPos dan pembagian tanggung jawab antar komponen agar developer mengetahui lokasi logic, alur request, dan batas tanggung jawab setiap bagian aplikasi.                                                                                                                                                                                                                                                                                                                             |
| Deskripsi          | AuliaPos menggunakan pola **MVC (Model-View-Controller) CodeIgniter 4 dengan Service Layer untuk logic aplikasi tertentu**. Controller menangani request dan orkestrasi workflow. Model menangani akses data dan aturan domain tertentu. Service menangani logic aplikasi tertentu yang dipisahkan dari Model. View menangani UI dan presentation logic sederhana. `Api.php` merupakan bagian dari Controller Layer dan digunakan untuk endpoint JSON/AJAX. `BaseController` merupakan base class untuk Controller. |
| Aturan Bisnis      | Aturan domain tidak seluruhnya berada pada satu layer. Sebagian aturan berada di Model, sebagian telah dipisahkan ke Service, dan sebagian workflow/validasi masih berada di Controller, termasuk `Api.php`. View dapat melakukan presentation logic sederhana, tetapi bukan sumber utama aturan domain.                                                                                                                                                                                                            |
| Alur               | Request browser masuk ke Controller. Controller memproses request dan menjalankan workflow dengan menggunakan Model dan/atau Service. Model berinteraksi dengan database utama. Untuk request AJAX/JSON, request dapat masuk melalui `Api.php` dan response dikembalikan dalam bentuk JSON. View digunakan untuk menghasilkan tampilan HTML.                                                                                                                                                                        |
| Hak Akses          | Hak akses diterapkan melalui authentication/filter dan validasi backend sesuai operasi. `Api.php` juga melakukan pemeriksaan hak akses pada endpoint tertentu. Penyembunyian tombol atau elemen UI pada View bukan pengganti validasi backend.                                                                                                                                                                                                                                                                      |
| Kondisi & Batasan  | Pemisahan Model dan Service mengikuti kondisi kode aktual. **Tidak semua business logic sudah dipisahkan dari Controller.** `Api.php` masih menangani sebagian validasi dan workflow bisnis. View diperbolehkan memiliki presentation logic sederhana seperti `if`, `foreach`, dan formatting. `Api.php` bukan architectural layer terpisah, melainkan Controller khusus endpoint JSON/AJAX.                                                                                                                        |
| Data yang Terlibat | Request HTTP, session/authentication context, data Model, data yang diproses Service, data transaksi dan master data, database utama MySQL/MariaDB, serta response HTML atau JSON. AuliaPos juga memiliki koneksi database SQLite terpisah untuk archive dan testing.                                                                                                                                                                                                                                               |
| Catatan            | `BaseController` menyediakan inisialisasi dan helper umum untuk Controller. Service yang bersifat stateless digunakan untuk logic tertentu yang dapat dipisahkan dan diuji secara independen. Arsitektur yang didokumentasikan menggambarkan struktur aktual AuliaPos v2.1, bukan target refactoring yang belum diterapkan.                                                                                                                                                                                         |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

| Nama Subbab        | Struktur Folder & Modul                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan struktur folder utama AuliaPos dan fungsi masing-masing folder agar developer dapat mengetahui lokasi kode berdasarkan jenis tanggung jawabnya.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Deskripsi          | AuliaPos menggunakan struktur folder CodeIgniter 4. Kode aplikasi utama berada di `app/`, aset yang dapat diakses browser berada di `public/`, pengujian berada di `tests/`, dokumentasi berada di `docs/`, dan file konfigurasi/dependency proyek berada di root repository. Di dalam `app/`, kode dipisahkan berdasarkan fungsi seperti Controller, Model, Service, View, Config, Filter, Helper, dan Database.                                                                                                                                                                                                    |
| Aturan Bisnis      | Struktur folder mengikuti pemisahan tanggung jawab kode, bukan pemisahan berdasarkan fitur bisnis. Modul bisnis seperti Kasir, Transaksi, Produk, Cash, dan Jadwal direpresentasikan melalui kombinasi Controller, Model, View, Service, dan konfigurasi terkait. Tidak semua modul memiliki folder khusus di setiap layer.                                                                                                                                                                                                                                                                                          |
| Alur               | Request aplikasi masuk melalui `public/index.php` sebagai entry point CodeIgniter, kemudian diarahkan melalui routing di `app/Config/Routes.php` ke Controller. Controller menggunakan Model dan/atau Service untuk menjalankan workflow, lalu menghasilkan View HTML atau response JSON. Data database dikelola melalui konfigurasi database dan komponen aplikasi terkait. Aset frontend berada di `public/assets/`.                                                                                                                                                                                               |
| Hak Akses          | Hak akses tidak ditentukan oleh struktur folder. Authentication dan pembatasan akses diterapkan melalui `app/Filters/AuthFilter.php`, routing, serta validasi backend pada Controller/endpoint yang relevan.                                                                                                                                                                                                                                                                                                                                                                                                         |
| Kondisi & Batasan  | Struktur yang didokumentasikan adalah struktur aktual AuliaPos v2.1. Folder `app/Services/` digunakan untuk logic aplikasi tertentu yang dipisahkan dari Model/Controller. `Api.php` berada di `app/Controllers/` karena merupakan bagian dari Controller Layer, bukan modul arsitektur terpisah. Folder View dikelompokkan berdasarkan area UI seperti `kasir`, `transaksi`, `produk`, `cash`, `jadwal`, dan lainnya. Tidak semua folder CodeIgniter harus dianggap sebagai modul bisnis.                                                                                                                           |
| Data yang Terlibat | Kode aplikasi pada `app/`, aset frontend pada `public/assets/`, database migration dan seed pada `app/Database/`, automated test pada `tests/`, serta dokumentasi teknis dan bisnis pada `docs/`.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Catatan            | Struktur utama yang perlu diketahui developer: `app/Controllers/` untuk request dan workflow, `app/Models/` untuk akses data dan aturan domain tertentu, `app/Services/` untuk logic aplikasi tertentu, `app/Views/` untuk UI, `app/Config/` untuk konfigurasi dan routing, `app/Filters/` untuk filter request seperti authentication, `app/Helpers/` untuk helper function, `app/Database/` untuk migration dan seed, `public/` untuk entry point dan aset publik, `tests/` untuk pengujian, dan `docs/` untuk dokumentasi. Struktur folder ini tidak berarti setiap business module memiliki satu folder mandiri. |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

| Nama Subbab        | Database & Struktur Tabel                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan database yang digunakan AuliaPos, struktur tabel utama, relasi antar tabel, serta aturan penting yang perlu dipahami developer ketika membaca atau mengubah data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Deskripsi          | AuliaPos menggunakan **MySQL/MariaDB** sebagai database utama dengan driver `MySQLi` dan charset `utf8mb4`. Struktur database didefinisikan melalui migration CodeIgniter 4 di `app/Database/Migrations/`. Baseline schema dibuat melalui `CreateAuliaPosCore`, kemudian beberapa migration berikutnya menambahkan perubahan seperti status `mangkrak`, snapshot diskon pelanggan, `priority` user, dan tabel `closing_kas`. Selain database utama, tersedia koneksi SQLite terpisah untuk archive transaksi dan SQLite in-memory khusus automated database tests.                                                                                   |
| Aturan Bisnis      | Tabel transaksi dan pembayaran memiliki dua lifecycle yang berbeda: `transaksi.status` menggambarkan status pekerjaan/transaksi, sedangkan `transaksi.status_pembayaran` menggambarkan status pembayaran. Pembayaran aktif disimpan di tabel `pembayaran` dan memiliki status `aktif` atau `reversed`. Relasi antar tabel menggunakan foreign key untuk menjaga integritas referensi. Data transaksi tidak boleh dianggap sama dengan data pembayaran hanya karena keduanya memiliki informasi status.                                                                                                                                               |
| Alur               | Data master seperti `users`, `kategori`, `pelanggan`, `bonus_rule`, dan `produk` menjadi referensi untuk data operasional. Transaksi disimpan pada `transaksi`, item transaksi pada `detail_transaksi`, dan pembayaran pada `pembayaran`. Data kas disimpan pada `cash_expense` dan `cash_opname`, sedangkan jadwal menggunakan `master_jadwal`, `master_jadwal_detail`, dan `jadwal`. Database view `v_daftar_pembayaran` dan `v_pembayaran_item_harian` digunakan untuk kebutuhan query pembayaran/laporan tertentu.                                                                                                                               |
| Hak Akses          | Database tidak menjadi satu-satunya tempat enforcement hak akses. Hak akses aplikasi diterapkan melalui authentication/filter dan validasi backend. Foreign key dan constraint database berfungsi menjaga **integritas data**, bukan menggantikan pemeriksaan role atau authorization di aplikasi.                                                                                                                                                                                                                                                                                                                                                   |
| Kondisi & Batasan  | Schema utama menggunakan MySQL/MariaDB dan migration CodeIgniter 4 sebagai sumber struktur database aplikasi. Migration baseline `CreateAuliaPosCore` mendefinisikan schema inti, sedangkan migration berikutnya bersifat incremental. Archive transaksi menggunakan SQLite terpisah dan bukan bagian dari database utama. Database testing menggunakan SQLite `:memory:` dan otomatis menjadi `defaultGroup` ketika environment adalah `testing`, sehingga pengujian tidak menggunakan database operasional.                                                                                                                                        |
| Data yang Terlibat | Tabel utama meliputi `users`, `bonus_rule`, `kategori`, `produk`, `pelanggan`, `transaksi`, `detail_transaksi`, `pembayaran`, `cash_expense`, `cash_opname`, `master_jadwal`, `master_jadwal_detail`, `jadwal`, dan `closing_kas`. Selain tabel terdapat database view `v_daftar_pembayaran` dan `v_pembayaran_item_harian`. Database utama menggunakan MySQL/MariaDB; archive dan testing menggunakan SQLite pada koneksi terpisah.                                                                                                                                                                                                                 |
| Catatan            | `transaksi` menjadi pusat data transaksi dan memiliki relasi ke `users` melalui `kasir_id` serta ke `pelanggan` melalui `pelanggan_id`. `detail_transaksi` memiliki relasi ke `transaksi` dan `produk`. `pembayaran` memiliki relasi ke `transaksi` dan `users`. `jadwal` serta `master_jadwal_detail` menggunakan `users` sebagai referensi karyawan. Beberapa foreign key menggunakan `ON DELETE CASCADE`, sedangkan relasi tertentu sengaja mempertahankan histori, misalnya `jadwal.karyawan_id` tidak menggunakan cascade delete. Perubahan schema harus dilakukan melalui migration, bukan dengan mengandalkan perubahan manual pada database. |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.**           

| Nama Subbab        | Routing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan bagaimana request HTTP diarahkan ke Controller dan method yang sesuai, termasuk aturan route, authentication, endpoint API, serta pembatasan akses berdasarkan role.                                                                                                                                                                                                                                                                                                                          |
| Deskripsi          | AuliaPos menggunakan **routing eksplisit CodeIgniter 4** yang didefinisikan di `app/Config/Routes.php`. Default controller diarahkan ke `Kasir` dengan method `index`. Aplikasi tidak menggunakan auto-routing sebagai mekanisme utama, sehingga endpoint yang tersedia ditentukan secara eksplisit pada konfigurasi route.                                                                                                                                                                               |
| Aturan Bisnis      | Route menentukan endpoint yang tersedia, tetapi route bukan satu-satunya tempat untuk menerapkan aturan bisnis. Authentication dan authorization tetap harus divalidasi pada backend. Operasi yang membutuhkan login menggunakan filter `auth`, sedangkan operasi tertentu juga melakukan pemeriksaan role, ownership, status transaksi, atau kondisi bisnis di Controller/Model/Service.                                                                                                                 |
| Alur               | Request HTTP masuk ke CodeIgniter → router mencocokkan URL dan HTTP method dengan route di `app/Config/Routes.php` → `AuthFilter` dijalankan pada route yang membutuhkan authentication → request diteruskan ke Controller dan method yang ditentukan → Controller menjalankan workflow dengan Model dan/atau Service → response dikembalikan sebagai HTML, redirect, atau JSON sesuai endpoint.                                                                                                          |
| Hak Akses          | Authentication menggunakan `AuthFilter` dengan alias `auth`. Filter memeriksa session login dan memiliki idle timeout 30 menit. Route administratif seperti `laporan`, `user-management`, `jadwal`, `produk`, `kategori`, `migrasi-manual`, `archive-transaksi`, dan `cash/closing` dibatasi untuk role admin. Pembatasan pada route tidak menggantikan validasi authorization di Controller atau layer aplikasi lainnya.                                                                                 |
| Kondisi & Batasan  | Routing utama harus mengikuti konfigurasi eksplisit di `app/Config/Routes.php`. `Api.php` merupakan bagian dari Controller Layer dan digunakan untuk endpoint JSON/AJAX, bukan architectural layer terpisah. Tidak semua aturan akses dapat disimpulkan hanya dari URL karena beberapa endpoint melakukan validasi tambahan di Controller. Perubahan route harus memperhatikan filter authentication, role access, HTTP method, dan Controller method yang menjadi target.                                |
| Data yang Terlibat | URL/path request, HTTP method, session authentication, role user, route parameters, Controller dan method tujuan, serta response HTML, redirect, atau JSON. Endpoint API menggunakan `app/Controllers/Api.php`, sedangkan endpoint halaman aplikasi menggunakan Controller terkait seperti `Kasir`, `Produk`, `Transaksi`, `Cash`, `Jadwal`, dan lainnya.                                                                                                                                                 |
| Catatan            | `app/Config/Routes.php` merupakan sumber utama definisi routing aplikasi. Default route menggunakan Controller `Kasir`. Route login/logout tidak membutuhkan authentication, sedangkan route aplikasi yang dilindungi menggunakan filter `auth`. `app/Filters/AuthFilter.php` menangani pemeriksaan session login, idle timeout, dan pembatasan route administratif berdasarkan role. `Api.php` menangani endpoint JSON/AJAX dan dapat memiliki validasi authorization tambahan di dalam method endpoint. |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

| Nama Subbab        | Controller                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan tanggung jawab Controller dalam menerima request, menjalankan workflow aplikasi, berkomunikasi dengan Model dan Service, serta menghasilkan response untuk pengguna atau endpoint API.                                                                                                                                                                                                                                          |
| Deskripsi          | AuliaPos menggunakan Controller sebagai bagian dari **Controller Layer** pada arsitektur MVC CodeIgniter 4. Controller menangani request HTTP dan orkestrasi workflow. Controller dapat menggunakan Model untuk akses data dan aturan domain tertentu, serta Service untuk logic aplikasi yang telah dipisahkan. `BaseController` menjadi base class yang digunakan oleh Controller aplikasi.                                               |
| Aturan Bisnis      | Controller bertanggung jawab menjalankan workflow, tetapi bukan satu-satunya tempat aturan bisnis. Aturan domain tertentu berada di Model, sedangkan logic tertentu yang dipisahkan berada di Service. Controller tidak boleh mengandalkan validasi pada View sebagai enforcement keamanan. Operasi yang sensitif harus tetap melakukan validasi backend terhadap authentication, role, ownership, status, dan kondisi bisnis yang relevan. |
| Alur               | Request HTTP diterima oleh routing CodeIgniter → route mengarahkan request ke Controller dan method → filter authentication dijalankan jika route membutuhkannya → Controller memvalidasi input dan kondisi akses → Controller menjalankan workflow dengan Model dan/atau Service → Controller mengembalikan response berupa View/HTML, redirect, atau JSON.                                                                                |
| Hak Akses          | Authentication diterapkan melalui `AuthFilter` pada route yang membutuhkan login. Controller juga dapat melakukan pemeriksaan authorization tambahan sesuai kebutuhan endpoint. Pada endpoint tertentu di `Api.php`, pemeriksaan session, role, ownership, atau kondisi seperti Shift Leader dilakukan di dalam Controller. Penyembunyian tombol pada View bukan pengganti validasi backend.                                                |
| Kondisi & Batasan  | Tidak semua business logic telah dipisahkan dari Controller. `Api.php` masih menangani sebagian validasi dan workflow bisnis. Controller dapat menggunakan Model dan Service secara langsung sesuai kebutuhan workflow. `Api.php` merupakan Controller khusus endpoint JSON/AJAX dan bukan layer arsitektur terpisah. `BaseController` menyediakan fungsi dan inisialisasi umum yang digunakan oleh Controller.                             |
| Data yang Terlibat | Request HTTP, session/authentication context, input form atau JSON, route parameters, Model, Service, database melalui Model, serta response HTML, redirect, atau JSON. Controller yang terkait mencakup area seperti `Kasir`, `Transaksi`, `Produk`, `Kategori`, `Pelanggan`, `Cash`, `Jadwal`, `Laporan`, `Tagihan`, dan `Api`.                                                                                                           |
| Catatan            | `app/Controllers/BaseController.php` merupakan base class yang mewarisi Controller CodeIgniter dan menginisialisasi helper umum seperti `cash`, `url`, dan `form`. `app/Controllers/Api.php` juga mewarisi `BaseController`. Controller tertentu menggunakan Service seperti `KalkulasiDiskonTransaksi` atau Service terkait lainnya. Dokumentasi ini menggambarkan struktur aktual AuliaPos `v2.1`, bukan target refactoring.              |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                               |
                                      
| Nama Subbab        | Model                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan tanggung jawab Model dalam AuliaPos, termasuk akses data, query khusus, validasi, aturan domain yang dipusatkan pada Model, serta hubungan Model dengan Service dan database.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Deskripsi          | AuliaPos menggunakan **Model CodeIgniter 4** pada `app/Models/`. Model merepresentasikan tabel atau kelompok data tertentu dan menjadi jalur utama aplikasi untuk membaca serta mengubah data database. Selain operasi CRUD, beberapa Model memiliki method khusus yang mengandung aturan domain, validasi, perhitungan, dan workflow data. Karena itu, Model AuliaPos tidak hanya berfungsi sebagai data access object sederhana.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Aturan Bisnis      | Aturan domain yang sudah dipusatkan pada Model harus digunakan sebagai sumber aturan ketika workflow terkait membutuhkan perubahan atau pembacaan data. `TransaksiModel` menjadi pusat aturan lifecycle transaksi dan pembayaran tertentu, termasuk validasi transisi status, sinkronisasi pembayaran, dan penambahan pembayaran. `PembayaranModel` menetapkan metode pembayaran yang valid dan hanya menghitung pembayaran dengan `status = aktif`. `JadwalModel` menjadi sumber definisi shift dan aturan jadwal aktual, sedangkan `MasterJadwalModel` menangani template jadwal yang terpisah dari jadwal aktual. Model lain menyediakan query dan operasi khusus sesuai domain masing-masing.                                                                                                                                                        |
| Alur               | Controller menerima request dan melakukan validasi/orchestrasi awal → Controller memanggil Model untuk membaca atau mengubah data → Model menjalankan query, validasi, atau aturan domain yang menjadi tanggung jawabnya → Model dapat menggunakan Service untuk logic tertentu yang memang dipisahkan → database menyimpan atau mengembalikan data → hasil dikembalikan ke Controller untuk menghasilkan response. Model juga dapat digunakan oleh Service atau komponen aplikasi lain sesuai kebutuhan.                                                                                                                                                                                                                                                                                                                                                |
| Hak Akses          | Model bukan pengganti authentication dan authorization. Pemeriksaan user login, role, ownership, serta kapabilitas kontekstual tetap dilakukan pada workflow aplikasi/Controller sesuai kebutuhan. Namun Model dapat memberikan **defense in depth** dengan membatasi field atau operasi tertentu. Contohnya `UserModel::updateProfilSaya()` hanya menerima `nama` dan `no_hp`, sehingga jalur tersebut tidak dapat digunakan untuk mengubah role, username, priority, atau field administratif lainnya.                                                                                                                                                                                                                                                                                                                                                 |
| Kondisi & Batasan  | Tidak semua business logic berada di Model. Logic yang bersifat stateless dan sengaja dipisahkan berada di `app/Services/`, misalnya `KalkulasiStatusPembayaran`, `KalkulasiDiskonTransaksi`, dan service lainnya. Sebaliknya, aturan domain yang membutuhkan konteks data atau menjadi chokepoint perubahan tetap berada di Model, terutama `TransaksiModel`. Controller tidak boleh membuat aturan yang bertentangan dengan aturan yang sudah dipusatkan di Model. View tidak boleh menjadi sumber kebenaran aturan domain.                                                                                                                                                                                                                                                                                                                            |
| Data yang Terlibat | Model utama meliputi `UserModel`, `ProdukModel`, `KategoriModel`, `PelangganModel`, `TransaksiModel`, `DetailTransaksiModel`, `PembayaranModel`, `CashExpenseModel`, `CashOpnameModel`, `ClosingKasModel`, `JadwalModel`, dan `MasterJadwalModel`. Model berinteraksi dengan tabel seperti `users`, `produk`, `kategori`, `pelanggan`, `transaksi`, `detail_transaksi`, `pembayaran`, `cash_expense`, `cash_opname`, `closing_kas`, `jadwal`, `master_jadwal`, dan `master_jadwal_detail`.                                                                                                                                                                                                                                                                                                                                                               |
| Catatan            | `TransaksiModel` merupakan Model dengan tanggung jawab domain paling besar. `ubahStatus()` menjadi enforcement lifecycle transaksi; `simpanTransaksi()` menangani penyimpanan transaksi beserta detail dalam database transaction; `sinkronkanPembayaran()` menghitung ulang total pembayaran efektif; dan `tambahPembayaran()` menjadi chokepoint penulisan pembayaran serta memvalidasi nominal, metode, tanggal, backdate, dan kondisi pembayaran. `PembayaranModel::getTotalDibayar()` hanya menghitung pembayaran aktif sehingga pembayaran `reversed` tetap menjadi histori tetapi tidak memengaruhi total efektif. `JadwalModel::DEFINISI_SHIFT` menjadi sumber kebenaran jam shift. `MasterJadwalModel` secara eksplisit memisahkan template mingguan dari jadwal aktual. `UserModel` juga menyediakan operasi khusus untuk password dan profil. |
| Status             | **Aktif — terverifikasi pada source code dan dokumentasi branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

| Nama Subbab        | Service & Business Logic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan fungsi Service Layer dalam AuliaPos, pembagian business logic antara Model dan Service, serta aturan mengenai logic yang dipusatkan, dipisahkan, dan digunakan lintas workflow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Deskripsi          | AuliaPos menggunakan **Service Layer untuk logic aplikasi tertentu** yang sengaja dipisahkan dari Controller dan/atau Model. Service berada di `app/Services/`. Sebagian Service bersifat **pure/stateless**, yaitu menerima input melalui parameter dan mengembalikan hasil tanpa membaca session, request, atau database. Service lainnya berinteraksi dengan database karena memang bertanggung jawab atas workflow aplikasi tertentu, seperti perhitungan saldo kas, penentuan Effective Shift Leader, dan archive transaksi. Service Layer bukan tempat seluruh business logic aplikasi; aturan domain tertentu tetap berada di Model, terutama `TransaksiModel`.                                                                                                                                                                                                                                                                                                                                     |
| Aturan Bisnis      | Business logic dipisahkan berdasarkan tanggung jawabnya. Logic perhitungan yang membutuhkan konsistensi dan sebelumnya berpotensi terduplikasi dipusatkan pada Service stateless, seperti `KalkulasiStatusPembayaran`, `KalkulasiDiskonTransaksi`, dan `KalkulasiJatuhTempo`. Logic authority dinamis berada pada `EffectiveShiftLeaderService` dan digunakan melalui `Authority`. Logic saldo kas berada pada `CashBalanceService`. Archive transaksi ditangani oleh `TransaksiArchiveService`. Service tidak boleh menciptakan aturan yang bertentangan dengan aturan domain yang sudah dipusatkan pada Model atau dokumentasi bisnis.                                                                                                                                                                                                                                                                                                                                                                   |
| Alur               | Controller atau Model membutuhkan logic tertentu → Service dipanggil dengan input yang diperlukan → Service menjalankan perhitungan atau workflow sesuai tanggung jawabnya → Service mengembalikan hasil kepada pemanggil. Untuk Service stateless, input diberikan secara eksplisit sehingga hasil dapat diuji secara deterministik. Untuk Service yang membutuhkan database, Service menggunakan koneksi database yang sesuai. Contohnya `TransaksiModel::sinkronkanPembayaran()` menggunakan `KalkulasiStatusPembayaran`, sedangkan `Authority::isCurrentShiftLeader()` menggunakan `EffectiveShiftLeaderService`.                                                                                                                                                                                                                                                                                                                                                                                      |
| Hak Akses          | Service tidak menjadi pengganti authentication. Hak akses tetap ditentukan oleh workflow pemanggil dan validasi backend. Untuk authority dinamis, `Authority` menyediakan pertanyaan khusus seperti apakah user merupakan admin atau Effective Shift Leader saat ini. `EffectiveShiftLeaderService` sendiri menentukan kandidat berdasarkan `users.role`, `is_active`, `priority`, jadwal, dan jendela waktu shift; tetapi status Shift Leader bukan role permanen di database. Service archive hanya digunakan melalui workflow administrasi archive transaksi.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Kondisi & Batasan  | **Tidak semua business logic dipindahkan ke Service.** `TransaksiModel::ubahStatus()` tetap menjadi enforcement lifecycle transaksi dan satu-satunya writer `status='selesai'`. `TransaksiModel::tambahPembayaran()` tetap menjadi chokepoint penulisan pembayaran. Service stateless tidak boleh bergantung pada session/request/database secara tersembunyi karena desainnya memang agar dapat diuji langsung. `KalkulasiStatusPembayaran` tidak melakukan clamping atau perubahan aturan lama; ia mempertahankan rule `dibayar >= grand_total → lunas`, `dibayar > 0 → dp`, selain itu `belum_bayar`. `KalkulasiDiskonTransaksi` memastikan diskon pelanggan dan diskon manual merupakan dua mode yang tidak digabung.                                                                                                                                                                                                                                                                                  |
| Data yang Terlibat | Service yang terimplementasi meliputi `Authority`, `CashBalanceService`, `EffectiveShiftLeaderService`, `EvaluasiJendelaKerjaShift`, `KalkulasiDiskonTransaksi`, `KalkulasiJatuhTempo`, `KalkulasiStatusPembayaran`, dan `TransaksiArchiveService`. Data yang digunakan mencakup transaksi, pembayaran aktif, pelanggan, cash expense, users, jadwal, priority, shift, serta database SQLite archive. Service kalkulasi stateless tidak membaca database/session/request dan hanya menggunakan parameter yang diberikan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Catatan            | `KalkulasiStatusPembayaran` menjadi sumber kebenaran perhitungan `status_pembayaran`. `KalkulasiDiskonTransaksi` menjadi sumber kebenaran perhitungan diskon dan `grand_total`, termasuk pembulatan Rp100 dan `selisih_pembulatan`. `KalkulasiJatuhTempo` menghitung jatuh tempo dari tanggal transaksi dan tempo tanpa menyimpan jatuh tempo ke database. `CashBalanceService` menghitung saldo kas secara real-time dari `cash_expense` dan pembayaran tunai aktif, bukan dari running ledger. `EffectiveShiftLeaderService` menghitung satu Shift Leader global secara on-demand berdasarkan priority dan jendela kerja. `EvaluasiJendelaKerjaShift` menggunakan `JadwalModel::DEFINISI_SHIFT` sebagai sumber jam kerja. `TransaksiArchiveService` memindahkan transaksi lama ke SQLite archive dengan proses backup, copy, validasi, lalu penghapusan dari database utama hanya setelah data archive terbukti lengkap. Setiap Service kalkulasi stateless memiliki unit test terkait di `tests/unit/`. |
| Status             | **Aktif — terverifikasi pada source code dan dokumentasi branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

| Nama Subbab        | API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan endpoint API/AJAX AuliaPos, format request dan response, tanggung jawab `Api.php`, authentication, authorization, serta batasan agar logic API tetap mengikuti aturan domain aplikasi.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Deskripsi          | AuliaPos memiliki `app/Controllers/Api.php` sebagai **Controller untuk endpoint JSON/AJAX**. API digunakan terutama oleh halaman POS/Kasir dan kebutuhan interaksi asynchronous seperti pencarian pelanggan, penyimpanan transaksi, pembayaran, perubahan status, dan pengambilan data operasional tertentu. `Api.php` bukan architectural layer terpisah; ia merupakan bagian dari **Controller Layer**. Endpoint API didefinisikan secara eksplisit di `app/Config/Routes.php` dan menggunakan filter `auth` pada route yang membutuhkan login.                                                                                                                                                                                                                                                                                                                                                                                                                |
| Aturan Bisnis      | API tidak boleh membuat aturan lifecycle transaksi atau pembayaran yang berbeda dari Model/Service. Perubahan status transaksi harus mengikuti `TransaksiModel::ubahStatus()`, sedangkan penulisan pembayaran harus melalui `TransaksiModel::tambahPembayaran()`. Status transaksi dan status pembayaran tetap merupakan dua sumbu independen. API yang menangani diskon transaksi menggunakan `KalkulasiDiskonTransaksi`, sehingga diskon pelanggan dihitung dari data master pelanggan di server, bukan mempercayai persentase dari browser. API juga melakukan validasi kondisi khusus seperti kepemilikan transaksi, `sumber`, role, Effective Shift Leader, status pembayaran, dan status transaksi sesuai endpoint.                                                                                                                                                                                                                                        |
| Alur               | Browser/POS mengirim request HTTP ke endpoint `/api/*` → route mencocokkan endpoint dan HTTP method → `auth` filter memastikan user sudah login → `Api.php` membaca input GET/POST/JSON sesuai endpoint → Controller melakukan validasi request dan authorization yang diperlukan → Model/Service menjalankan operasi data atau business logic → API mengembalikan response JSON kepada client. Untuk workflow transaksi, API dapat meneruskan operasi ke `TransaksiModel` sehingga aturan domain tetap dipusatkan pada Model.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Hak Akses          | Endpoint API pada branch `v2.1` umumnya dilindungi filter `auth`. Authentication berbasis session dan role dasar adalah `admin` atau `kasir`. Authorization tambahan dilakukan pada endpoint sesuai konteks. Contohnya penyelesaian transaksi melalui `/api/kasir/selesaikan-transaksi` memiliki pemeriksaan khusus workflow POS dan ownership; workflow umum `/api/ubah-status` menggunakan aturan `TransaksiModel::ubahStatus()` dan dapat melibatkan Effective Shift Leader. Endpoint tertentu seperti `GET /roster/shift-leader-saat-ini` bersifat read-only untuk user yang login. UI tidak dianggap sebagai enforcement; backend API tetap memvalidasi ulang kondisi akses.                                                                                                                                                                                                                                                                                |
| Kondisi & Batasan  | `Api.php` **masih mengandung sebagian validasi dan workflow bisnis**, sehingga pemisahan business logic ke Model/Service belum total. Namun aturan lifecycle yang sudah dipusatkan tidak boleh diduplikasi dengan aturan berbeda di API. Response API menggunakan JSON dan bukan View HTML untuk endpoint AJAX. API harus mengikuti HTTP method yang didefinisikan di `Routes.php`. Input dari browser tidak boleh dipercaya sebagai sumber kebenaran untuk data sensitif atau aturan bisnis; contoh pentingnya adalah diskon pelanggan, status pembayaran, ownership, dan authority Shift Leader.                                                                                                                                                                                                                                                                                                                                                               |
| Data yang Terlibat | Request GET/POST/JSON, session `id_user` dan role, data pelanggan, produk, transaksi, detail transaksi, pembayaran, users, jadwal, serta hasil Service seperti kalkulasi diskon dan Effective Shift Leader. Response API umumnya berupa JSON dengan informasi seperti `status`, `message`, `data`, `id`, atau hasil operasi sesuai endpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Catatan            | Endpoint yang diverifikasi pada routing mencakup `POST /api/simpan-transaksi`, `GET /api/modal/pembayaran`, dan `POST /api/kasir/selesaikan-transaksi`; `Api.php` juga memiliki endpoint AJAX lain untuk pelanggan, transaksi, pembayaran, produk, status, Shift Leader, dan kebutuhan operasional lainnya sesuai konfigurasi route lengkap. `Api::simpanTransaksi()` menerima cart dari browser, memvalidasi kebutuhan No Order, pelanggan, metode pembayaran, DP/piutang/draft, kemudian menggunakan `KalkulasiDiskonTransaksi` dan `TransaksiModel::simpanTransaksi()`. Pembayaran yang dihasilkan selanjutnya mengikuti chokepoint `TransaksiModel::tambahPembayaran()`. `Api.php` juga menggunakan `PembayaranModel::METODE` sebagai daftar metode pembayaran yang valid. Dokumentasi API harus dibaca bersama `Routes.php`, `TransaksiModel`, `PembayaranModel`, `Authority`, dan `AULIA.md`, karena kontrak API tidak berdiri sendiri dari aturan domain. |
| Status             | **Aktif — terverifikasi pada source code dan dokumentasi branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Nama Subbab        | Authentication & Authorization                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan mekanisme autentikasi pengguna, session, timeout, role, pembatasan akses route, serta authorization dinamis yang digunakan AuliaPos untuk memastikan setiap operasi hanya dapat dilakukan oleh pihak yang berwenang.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Deskripsi          | AuliaPos menggunakan **session-based authentication** tanpa library authentication khusus. Proses login dilakukan oleh `Auth::prosesLogin()` dengan mengambil user berdasarkan username melalui `UserModel`, kemudian memverifikasi password terhadap `password_hash`. Jika berhasil, sistem menyimpan konteks user ke session seperti `id_user`, `username`, `inisial`, `role`, `isLoggedIn`, dan `last_activity`. Route yang membutuhkan login menggunakan filter `auth` yang terdaftar melalui `AuthFilter`. Role dasar aplikasi hanya **`admin` dan `kasir`**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Aturan Bisnis      | User harus login sebelum mengakses route yang dilindungi. Session memiliki idle timeout **30 menit**; setelah melewati batas tersebut session dihancurkan dan user harus login kembali. `admin` memiliki akses administratif, sedangkan `kasir` dibatasi dari route administratif yang didefinisikan dalam `AuthFilter::$adminRoutes`. Authorization tidak hanya bergantung pada role: operasi tertentu menggunakan pemeriksaan tambahan seperti ownership transaksi, status transaksi, status pembayaran, `sumber`, dan **Effective Shift Leader**. Effective Shift Leader bukan role permanen dan tidak pernah ditulis sebagai `users.role = 'shift_leader'`; authority tersebut dihitung secara dinamis dari Priority, jadwal, status aktif, dan jendela kerja shift.                                                                                                                                                                                                                                                                                              |
| Alur               | User membuka halaman login → `Auth::login()` menampilkan form → `Auth::prosesLogin()` menerima username/password → `UserModel` mencari user dan memverifikasi password → session authentication dibuat → user diarahkan sesuai role (`admin` ke `/laporan`, `kasir` ke `/kasir`) → request berikutnya melewati `AuthFilter` → filter memeriksa `isLoggedIn`, timeout, dan route admin → jika valid request diteruskan ke Controller → untuk operasi yang membutuhkan authority tambahan, Controller/Model menggunakan pemeriksaan authorization yang lebih spesifik seperti `Authority::isAdmin()` atau `Authority::isCurrentShiftLeader()`. Logout menghancurkan seluruh session dan mengarahkan user kembali ke login.                                                                                                                                                                                                                                                                                                                                              |
| Hak Akses          | **Admin** dapat mengakses route administratif seperti laporan, manajemen user, produk, kategori, jadwal, migrasi manual, archive transaksi, dan closing kas. **Kasir** dapat menggunakan workflow operasional yang tidak termasuk dalam daftar admin-only. Selain role dasar, beberapa aksi diberikan kepada Effective Shift Leader secara eksplisit. Contohnya `PROSES → SELESAI`, pembatalan transaksi, backdate payment, dan akses `GET /api/kasir-list`. Kapabilitas tersebut tidak otomatis berlaku hanya karena seseorang memiliki Priority; user harus memenuhi seluruh syarat Effective Shift Leader saat request berlangsung.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Kondisi & Batasan  | Authentication dan authorization merupakan dua hal berbeda: berhasil login tidak berarti user memiliki izin untuk semua operasi. `AuthFilter` memberikan **route-level gate**, tetapi enforcement business rule dilakukan kembali pada Controller/Model terkait. Penyembunyian tombol di View hanya lapisan UI dan bukan mekanisme keamanan. Tidak terdapat permission matrix atau `hasAuthority()` generik. `Authority` hanya menyediakan pertanyaan authority yang memang dibutuhkan, yaitu `isAdmin()` dan `isCurrentShiftLeader()`. Effective Shift Leader dihitung **on-demand**, tidak disimpan atau di-cache. Jika tidak ada kandidat yang memenuhi syarat, hasilnya tidak ada Shift Leader.                                                                                                                                                                                                                                                                                                                                                                   |
| Data yang Terlibat | Data authentication berasal dari session (`id_user`, `username`, `inisial`, `role`, `isLoggedIn`, `last_activity`) dan data akun pada tabel `users`, termasuk `password_hash`, `role`, `is_active`, dan `priority`. Authorization dinamis juga menggunakan data `jadwal` serta definisi jam shift pada `JadwalModel::DEFINISI_SHIFT`. Untuk authorization transaksi, data `kasir_id`, `sumber`, `status`, dan `status_pembayaran` ikut digunakan.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Catatan            | `AuthFilter` diterapkan **per route**, bukan secara global. Route admin ditentukan melalui daftar prefix di `AuthFilter::$adminRoutes`; karena itu struktur URI harus dipertimbangkan ketika menambah route baru. Prefix `/roster` sengaja dipisahkan dari `/jadwal` agar informasi roster yang read-only dapat diakses kasir, sedangkan mutation `/jadwal/*` tetap admin-only. `Authority::isCurrentShiftLeader()` menggunakan `EffectiveShiftLeaderService` dan bersifat fail-closed: jika terjadi error ketika menentukan authority, hasilnya `false`, bukan memberikan akses. `UserModel` juga menjadi bagian penting dari keamanan akun, termasuk hashing/verifikasi password dan aturan perubahan profil. Pada manajemen user, admin tidak dapat mengubah akun dirinya sendiri menjadi non-admin atau menonaktifkan akun sendiri. Priority bersifat unik dan tidak mengubah role dasar. Enforcement transaksi tetap dipusatkan pada `TransaksiModel`, sehingga API/controller tidak boleh membuat jalur authorization yang bertentangan dengan aturan tersebut. |
| Status             | **Aktif — terverifikasi pada source code dan dokumentasi branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

| Nama Subbab        | Frontend & JavaScript                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan struktur frontend AuliaPos, pembagian JavaScript bersama dan spesifik halaman, pengelolaan state di browser, komunikasi dengan backend/API, serta batas tanggung jawab frontend terhadap business rule dan keamanan aplikasi.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Deskripsi          | Frontend AuliaPos menggunakan View CodeIgniter 4 sebagai HTML server-rendered dengan JavaScript untuk interaksi dinamis. Logic bersama halaman POS ditempatkan di `public/assets/js/kasir-shared.js` dan digunakan oleh `app/Views/kasir/index.php` serta `app/Views/kasir/edit.php`. Logic pembayaran dipisahkan ke `public/assets/js/payment.js` karena modal pembayaran digunakan pada beberapa workflow. Frontend menggunakan JavaScript native dan jQuery/AJAX pada bagian yang sudah ada, serta Bootstrap untuk komponen modal. JavaScript berkomunikasi dengan endpoint backend melalui HTTP/AJAX/`fetch`, sementara data awal tertentu dikirim dari PHP View ke JavaScript melalui konfigurasi halaman.                                                                                                                                                                                                                                                                                                   |
| Aturan Bisnis      | Frontend bertanggung jawab atas **UI, state sementara, validasi UX, kalkulasi preview, dan interaksi pengguna**, tetapi bukan sumber kebenaran utama business rule. Contohnya `kasir-shared.js` menghitung preview subtotal, diskon, pembulatan, dan total untuk memberikan feedback langsung kepada kasir, tetapi nilai final transaksi tetap dihitung dan divalidasi di backend. `payment.js` mengatur alur modal pembayaran, nominal, kembalian, DP, dan backdate dari sisi UI; backend tetap melakukan validasi pembayaran dan authorization. Diskon pelanggan yang ditampilkan di browser juga bukan authority final: persentase diskon pelanggan dibaca ulang dari database ketika transaksi disimpan.                                                                                                                                                                                                                                                                                                      |
| Alur               | View menyiapkan HTML dan konfigurasi JavaScript → script bersama dimuat → state awal seperti `cart` dibentuk dari data halaman atau `sessionStorage` → user melakukan interaksi → JavaScript memperbarui state dan DOM → untuk operasi yang membutuhkan server, JavaScript mengirim request ke endpoint API/route → backend melakukan validasi dan business logic → response JSON diterima → JavaScript memperbarui UI, menampilkan toast/modal, atau mengubah state lokal. Pada halaman edit transaksi, data transaksi digunakan untuk mengisi cart awal; pada halaman POS baru, cart dapat dipersist sementara menggunakan `sessionStorage`.                                                                                                                                                                                                                                                                                                                                                                    |
| Hak Akses          | JavaScript tidak memberikan hak akses keamanan. UI dapat menyembunyikan, menonaktifkan, atau tidak menampilkan aksi tertentu berdasarkan kondisi halaman, tetapi backend tetap menjadi enforcement. Endpoint yang dipanggil JavaScript tetap melewati authentication dan authorization backend. Dengan demikian manipulasi JavaScript, request manual, atau perubahan nilai di browser tidak boleh dianggap sebagai bukti bahwa user berhak melakukan operasi.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Kondisi & Batasan  | `kasir-shared.js` sengaja **tidak menggunakan IIFE/module** karena fungsi-fungsinya dipanggil dari atribut event seperti `onclick` dan `oninput` pada View/modal. Akibatnya fungsi dan variabel tertentu berada pada global scope dan menjadi kontrak dengan halaman pemanggil. Sebelum script dimuat, `index.php`/`edit.php` harus menyediakan `KASIR_CONFIG` dan data produk yang dibutuhkan. `cart` merupakan satu array bersama yang juga digunakan oleh `payment.js` dan komponen modal produk. `sessionStorage` hanya digunakan sebagai penyimpanan state sementara browser pada workflow POS tertentu; pada `edit.php`, cart berasal dari data transaksi dan tidak dipersist dengan mekanisme tersebut. Frontend tidak boleh dijadikan tempat enforcement lifecycle transaksi, payment authorization, ownership, atau permission.                                                                                                                                                                          |
| Data yang Terlibat | State frontend meliputi `cart`, pelanggan terpilih, diskon, No Order, state modal pembayaran, nominal pembayaran, DP, kembalian, dan konfigurasi URL endpoint. `kasir-shared.js` juga menangani item custom, banner, manual product, dan data detail banner. `payment.js` memiliki state workflow pembayaran seperti `mode`, `transaksiId`, `total`, `sisa`, `existingFlow`, `dpAmount`, status submit, serta data backdate (`backdateMode`, `backdateTanggal`, `backdateKasirId`). Data sementara cart dapat disimpan pada `sessionStorage` dengan key `aulia_kasir_cart_v1`.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Catatan            | `kasir-shared.js` merupakan shared frontend contract untuk `kasir/index.php` dan `kasir/edit.php`; perubahan terhadap fungsi, nama global, atau struktur `KASIR_CONFIG` perlu mempertimbangkan kedua halaman dan komponen yang memanggilnya. Script menangani pencarian pelanggan, cart, No Order, diskon, banner, produk custom/manual, rendering UI, dan berbagai helper POS. `payment.js` memusatkan perilaku modal pembayaran agar tidak diduplikasi antarhalaman; modal dapat dimuat secara asynchronous melalui `fetch` dan menggunakan `credentials: "same-origin"`. Script juga memiliki mekanisme untuk mencegah submit pembayaran ganda melalui state `submitting`. Validasi frontend seperti batas jumlah item, format input, uang kurang, atau kebutuhan No Order terutama merupakan UX guard; validasi final tetap berada di backend. Dokumentasi developer menegaskan bahwa perubahan pada shared JavaScript harus mempertahankan penggunaan bersama antara `kasir/index.php` dan `kasir/edit.php`. |
| Status             | **Aktif — terverifikasi pada source code dan dokumentasi branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

| Nama Subbab        | Integrasi Library / Dependency                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan dependency eksternal dan komponen library yang digunakan AuliaPos, fungsi masing-masing dependency, titik integrasinya dengan aplikasi, serta batasan penggunaan agar perubahan dependency tidak merusak workflow inti.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Deskripsi          | AuliaPos dibangun menggunakan **CodeIgniter 4** sebagai framework utama dengan PHP **8.2+**. Dependency production dikelola melalui Composer dan tercatat di `composer.json`. Library utama yang digunakan adalah `codeigniter4/framework`, `dompdf/dompdf`, dan `mike42/escpos-php`. Dependency development/testing meliputi PHPUnit, Faker, dan VFS Stream. Selain dependency Composer, aplikasi juga menggunakan database driver MySQLi untuk database utama, SQLite3 untuk archive dan database test, serta tool eksternal server-side untuk kebutuhan printing langsung.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Aturan Bisnis      | Dependency digunakan berdasarkan tanggung jawab teknisnya dan tidak boleh menjadi sumber aturan bisnis yang bertentangan dengan dokumentasi AuliaPos. CodeIgniter menangani framework MVC, HTTP request/response, routing, session, database abstraction, migration, dan konfigurasi. Dompdf hanya digunakan untuk menghasilkan PDF nota; business data tetap berasal dari Model dan View AuliaPos. `mike42/escpos-php` digunakan untuk komunikasi printer thermal melalui ESC/POS. PHPUnit digunakan untuk memastikan logic yang dipisahkan, terutama Service stateless, tetap sesuai invariant bisnis. Archive menggunakan koneksi SQLite terpisah dan tidak menggantikan database operasional MySQL.                                                                                                                                                                                                                                                                                                                                        |
| Alur               | Aplikasi dijalankan oleh CodeIgniter 4 → Composer menyediakan framework dan library melalui autoload → Controller/Service menggunakan dependency sesuai kebutuhan → Model menggunakan database connection CodeIgniter → fitur printing menggunakan Dompdf untuk membentuk PDF atau `escpos-php` untuk printer thermal → archive menggunakan connection `archive` berbasis SQLite → automated test menggunakan PHPUnit dengan koneksi database test SQLite ketika diperlukan. Dependency yang membutuhkan konfigurasi environment mengambil nilai dari Config class atau `.env`, bukan dari request browser.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Hak Akses          | Dependency tidak menentukan authorization aplikasi. Authentication dan authorization tetap ditangani oleh mekanisme AuliaPos seperti `AuthFilter`, Controller, `Authority`, dan Model. Fitur yang menggunakan dependency sensitif, terutama archive dan server-side printing, tetap dipanggil melalui workflow yang sudah memiliki pembatasan akses. Konfigurasi printer langsung berada di server melalui `App\Config\PrintNota`; browser tidak diperbolehkan menentukan executable atau printer tujuan melalui request.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Kondisi & Batasan  | Dependency production dan development dipisahkan melalui `require` dan `require-dev` pada `composer.json`. CodeIgniter menggunakan PHP `^8.2` dan framework `^4.7`. Dompdf membutuhkan dukungan PHP image extension (`gd` atau `imagick`) untuk rendering gambar pada nota PDF; implementasi `Cetak` melakukan pemeriksaan ini secara eksplisit. Server-side direct printing membutuhkan executable printer tool yang dikonfigurasi pada server serta printer yang dapat dijangkau server. `escpos-php` membutuhkan koneksi printer yang sesuai; konfigurasi thermal saat ini menggunakan `WindowsPrintConnector` dengan target SMB. Archive menggunakan SQLite3 dan connection group terpisah sehingga tidak boleh dianggap sebagai database utama.                                                                                                                                                                                                                                                                                           |
| Data yang Terlibat | Dependency berinteraksi dengan request/response HTTP, session, database MySQL/MariaDB utama, database SQLite archive, database SQLite in-memory untuk testing, HTML View nota, binary PDF, koneksi printer ESC/POS, konfigurasi printer, serta file temporary hasil rendering PDF. Composer metadata di `composer.json` menjadi sumber daftar dependency dan versinya.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Catatan            | Dependency production yang terverifikasi: **CodeIgniter 4 (`codeigniter4/framework`)**, **Dompdf (`dompdf/dompdf`)**, dan **mike42/escpos-php**. Dependency development: **PHPUnit 10.5.16+**, **FakerPHP**, dan **mikey179/vfsstream**. `Cetak` mengintegrasikan Dompdf untuk PDF nota A6 landscape dan `escpos-php` untuk thermal printing. Direct print menggunakan tool command-line yang dikonfigurasi melalui `PrintNota`; path executable dan printer target tidak berasal dari request. `Database` memiliki connection `default` untuk MySQL/MariaDB, `archive` untuk SQLite archive, dan `tests` untuk SQLite in-memory. Saat environment adalah `testing`, `defaultGroup` otomatis diarahkan ke connection `tests` untuk mencegah test menulis ke database operasional. Composer menyediakan script `composer test` yang menjalankan PHPUnit. Dependency baru sebaiknya ditambahkan hanya jika memang diperlukan dan harus diperiksa dampaknya terhadap deployment, konfigurasi server, testing, dan kompatibilitas PHP/CodeIgniter. |
| Status             | **Aktif — terverifikasi pada source code dan dokumentasi branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

| Nama Subbab        | Konfigurasi Aplikasi                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan lokasi, mekanisme, dan parameter konfigurasi AuliaPos yang mengatur URL aplikasi, environment, database, timezone, helper, routing, printing, serta pemisahan konfigurasi production dan testing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Deskripsi          | Konfigurasi aplikasi AuliaPos mengikuti struktur konfigurasi **CodeIgniter 4** dan berada terutama di `app/Config/`. Konfigurasi runtime dapat menggunakan file `.env`, sedangkan `app/Config/*.php` menyediakan nilai default dan struktur konfigurasi aplikasi. Repository tidak menyimpan `.env` aktual. Setup pada `README.md` mengharuskan developer membuat `.env` dari template `env` kemudian mengisi `baseURL` dan konfigurasi database. Konfigurasi penting tersebar pada `App.php`, `Database.php`, `PrintNota.php`, `Routes.php`, `Events.php`, `Autoload.php`, dan konfigurasi framework lainnya.                                                                                                                                                                                                                                                                                                                                                |
| Aturan Bisnis      | Konfigurasi tidak boleh mengubah aturan domain secara diam-diam. Parameter environment digunakan untuk menyesuaikan deployment seperti URL aplikasi, kredensial database, lokasi database archive, dan target printer, sedangkan aturan bisnis tetap berada pada Model, Service, dan dokumentasi bisnis. Database utama menggunakan connection `default`; database archive menggunakan connection `archive`; dan test menggunakan connection `tests`. Konfigurasi environment testing secara otomatis mengarahkan `defaultGroup` ke `tests` agar test tidak menulis ke database operasional. Konfigurasi printer langsung tetap berasal dari server dan tidak boleh berasal dari request browser.                                                                                                                                                                                                                                                             |
| Alur               | Aplikasi CodeIgniter melakukan bootstrap → konfigurasi `app/Config` dimuat → nilai yang dapat dioverride dari `.env` diterapkan → framework menentukan environment dan koneksi yang digunakan → Controller/Model/Service menggunakan konfigurasi tersebut melalui Config class atau database service → workflow berjalan menggunakan konfigurasi environment yang aktif. Saat PHPUnit dijalankan dalam environment `testing`, `Database::__construct()` mengubah `defaultGroup` menjadi `tests`. Untuk archive atau printing, aplikasi menggunakan connection/configuration khusus yang terpisah dari database operasional.                                                                                                                                                                                                                                                                                                                                   |
| Hak Akses          | Konfigurasi tidak menggantikan authorization aplikasi. Route menggunakan filter `auth`, sedangkan pembatasan admin dan authorization bisnis dilakukan oleh `AuthFilter`, Controller, `Authority`, dan Model. Parameter sensitif seperti kredensial database dan konfigurasi printer harus berada pada environment/config server, bukan dikirim dari browser. Fitur migrasi manual dan archive tetap mengikuti pembatasan admin yang sudah ditentukan pada routing dan authentication.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Kondisi & Batasan  | `App.php` menetapkan `baseURL` default `http://localhost/aulia/` dan timezone aplikasi `Asia/Jakarta`. `getBaseURL()` dapat melakukan deteksi berdasarkan request apabila `$baseURL` kosong. Database utama menggunakan `MySQLi` dengan charset `utf8mb4`; connection archive menggunakan `SQLite3`; connection testing menggunakan SQLite in-memory. `PrintNota` menyediakan konfigurasi printer langsung, executable PDF printer, print settings, dan timeout; parameter tersebut dapat dioverride melalui `.env`. `Routes.php` berisi route eksplisit dan tidak menggunakan auto-routing. `Autoload.php` memetakan namespace `App` dan memuat helper `order`, `cash`, `url`, dan `form`. `Events.php` memiliki bootstrap event untuk output compression, debug toolbar, dan hot reload pada development.                                                                                                                                                   |
| Data yang Terlibat | Parameter konfigurasi meliputi `baseURL`, hostname/username/password/database database utama, database archive, database testing, DB driver, charset, timezone, environment, path migrations/seeds, helper autoload, konfigurasi printer langsung, executable printing, print settings, timeout printing, serta konfigurasi routing/filter. Database archive default berada di `WRITEPATH/archive/aulia_pos_archive.db` dan dapat dioverride melalui parameter `database.archive.database`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Catatan            | `.env` aktual tidak menjadi bagian repository dan karena itu nilainya tidak didokumentasikan sebagai nilai runtime. `README.md` menyediakan alur setup `composer install`, membuat `.env` dari template `env`, lalu mengisi `baseURL` dan `database.default.*`. `App.php` masih memiliki default development `http://localhost/aulia/`, sehingga deployment non-local perlu memastikan `baseURL` yang benar. `Database.php` memiliki tiga kelompok koneksi penting: `default` untuk database operasional, `archive` untuk SQLite archive, dan `tests` untuk PHPUnit. `PrintNota.php` sengaja memisahkan konfigurasi printer langsung dari controller dan memungkinkan override melalui `.env`; target printer dan executable tetap server-side. Konfigurasi aplikasi harus diperlakukan sebagai bagian dari deployment: perubahan kredensial, URL, database, printer, atau environment perlu diverifikasi terhadap workflow terkait sebelum dianggap selesai. |
| Status             | **Aktif — terverifikasi pada source code dan dokumentasi branch ****`v2.1`****.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

| Nama Subbab        | Environment & Secrets                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menjelaskan cara AuliaPos mengelola perbedaan konfigurasi antar-environment serta menangani nilai sensitif seperti credential database dan konfigurasi server, tanpa menanamkan secret ke source code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Deskripsi          | AuliaPos menggunakan mekanisme konfigurasi CodeIgniter 4 melalui file `.env` untuk nilai yang dapat berbeda antar-environment. Repository menyediakan template `env` yang disalin menjadi `.env` saat setup. `README.md` secara eksplisit meminta developer mengisi `baseURL` dan parameter `database.default.*`. Konfigurasi aplikasi kemudian menggunakan nilai tersebut melalui mekanisme environment CodeIgniter.                                                                                                                                                                                                                                                                                                                                                                                              |
| Aturan Bisnis      | `.env` bukan tempat menyimpan aturan bisnis. File tersebut digunakan untuk konfigurasi deployment dan nilai yang berbeda antar-environment. Credential database, path server, printer share, dan lokasi executable tidak boleh dijadikan input dari browser. Untuk fitur cetak langsung, konfigurasi printer dan executable tetap berasal dari server melalui `Config\PrintNota`, meskipun dapat dioverride melalui `.env`.                                                                                                                                                                                                                                                                                                                                                                                        |
| Alur               | Developer menyalin `env` menjadi `.env` → mengisi konfigurasi environment lokal/server → CodeIgniter membaca environment tersebut → `app/Config/*` menggunakan konfigurasi terkait → aplikasi berjalan menggunakan nilai environment tersebut. Untuk testing, `Database.php` secara otomatis mengubah `defaultGroup` menjadi `tests` ketika `ENVIRONMENT === 'testing'`, sehingga test tidak menggunakan database operasional.                                                                                                                                                                                                                                                                                                                                                                                     |
| Hak Akses          | `.env` dan credential database merupakan konfigurasi server, bukan data yang boleh dikirim atau dikendalikan oleh client. Browser tidak menentukan printer path, executable path, atau koneksi database. Authentication aplikasi tetap menggunakan session melalui `AuthFilter`; `.env` tidak menggantikan mekanisme authorization aplikasi.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Kondisi & Batasan  | File `.env` aktual tidak tersedia di repository sehingga nilai secret/runtime aktual tidak dapat dianggap sebagai bagian dari source yang terverifikasi. `Database.php` memiliki default connection MySQLi untuk database utama, connection `archive` menggunakan SQLite, dan connection `tests` menggunakan SQLite in-memory. `database.archive.database` dapat dioverride melalui `.env`. `PrintNota` menyediakan override `.env` untuk `printnota.printerLangsung`, `printnota.sumatraPdfPath`, dan konfigurasi print terkait. Nilai `CI_ENVIRONMENT` menentukan environment aplikasi; jangan menggunakan environment testing untuk database operasional.                                                                                                                                                       |
| Data yang Terlibat | `CI_ENVIRONMENT`, `app.baseURL`, `database.default.*`, `database.archive.database`, konfigurasi `printnota.*`, credential database, path executable server, path file archive, serta konfigurasi database testing. Database utama menggunakan MySQLi/MariaDB; archive menggunakan SQLite; testing menggunakan SQLite in-memory.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Catatan            | Parameter utama yang perlu diperhatikan developer adalah: `CI_ENVIRONMENT`, `app.baseURL`, `database.default.hostname`, `database.default.database`, `database.default.username`, `database.default.password`, `database.default.port`, `database.archive.database`, `printnota.printerLangsung`, `printnota.sumatraPdfPath`, `printnota.printSettings`, dan `printnota.timeoutDetik`. `README.md` menggunakan `aulia_kasirdb` sebagai nama database operasional pada dokumentasi setup. `CLAUDE.md` juga menegaskan bahwa konfigurasi direct printing (`printnota.*`, SumatraPDF path, printer share) berasal dari `.env`/`app/Config/PrintNota.php`. CodeIgniter memang mendukung konfigurasi environment melalui `.env`; penggunaan `.env` untuk konfigurasi deployment merupakan pola yang didukung framework. |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.** Struktur dan parameter konfigurasi diverifikasi dari source; secret aktual tidak diverifikasi karena `.env` aktual tidak berada di repository.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

| Nama Subbab        | Migration Database                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan struktur migration database AuliaPos, urutan perubahan schema, cara menjalankan migration, batas antara migration dan seed, serta aturan aman ketika melakukan perubahan database.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Deskripsi          | AuliaPos v2.1 menggunakan **CodeIgniter 4 Database Migration**. Migration berada di `app/Database/Migrations/` dan dikelola oleh MigrationRunner CodeIgniter. Baseline schema dibuat oleh `2026-09-08-000001_CreateAuliaPosCore.php`, kemudian perubahan schema berikutnya dibuat sebagai migration terpisah. Migration baseline sengaja merepresentasikan schema core lengkap berdasarkan struktur database AuliaPos yang telah diverifikasi, bukan kumpulan `ALTER TABLE` historis dari versi lama.                                                                                                                                                       |
| Aturan Bisnis      | Migration hanya bertanggung jawab terhadap **struktur database**, bukan data operasional. Migration tidak melakukan seed customer, transaksi, pembayaran, kas, atau jadwal. Data master awal ditangani terpisah oleh `AuliaPosInitialSeeder`. Database utama tetap MySQL/MariaDB; migration tidak mengubah database archive SQLite sebagai bagian dari schema operasional. Tabel `migrations` bukan dibuat oleh aplikasi migration AuliaPos karena tabel tersebut dimiliki dan dikelola oleh framework CodeIgniter.                                                                                                                                         |
| Alur               | Setup baru: konfigurasi database → `php spark migrate` → migration baseline dan migration pending dijalankan sesuai urutan versi → database siap digunakan → `php spark db:seed AuliaPosInitialSeeder` bila membutuhkan data master → aplikasi dapat dijalankan. Pada hosting tanpa akses CLI, route `/migrasi-manual` menyediakan alternatif untuk admin. Controller tersebut memanggil `Services::migrations()->latest()`, sehingga hanya migration yang masih pending yang dijalankan.                                                                                                                                                                   |
| Hak Akses          | Migration normal melalui CLI bergantung pada akses server/CLI. Fitur `/migrasi-manual` dibatasi **admin-only** melalui `AuthFilter` dan pemeriksaan admin tambahan di controller. Endpoint eksekusi membutuhkan konfirmasi POST `JALANKAN`. Migration tidak menerima kode SQL migration dari browser; kode yang dijalankan berasal dari file migration yang sudah berada di `app/Database/Migrations/`.                                                                                                                                                                                                                                                     |
| Kondisi & Batasan  | Migration baseline tidak boleh diperlakukan sebagai migration incremental biasa. `CreateAuliaPosCore` membuat schema lengkap dan `down()` menghapus seluruh tabel core beserta view. Migration berikutnya melakukan perubahan terisolasi: menambahkan status `mangkrak`, snapshot persentase diskon pelanggan, `users.priority`, dan tabel `closing_kas`. Rollback migration dapat gagal secara sengaja apabila data existing tidak kompatibel dengan schema lama; contoh migration `AddStatusMangkrakTransaksi::down()` tidak mengubah diam-diam transaksi yang sudah berstatus `mangkrak`. Backup database tetap disarankan sebelum migration production. |
| Data yang Terlibat | Migration schema mencakup tabel core seperti `users`, `kategori`, `pelanggan`, `produk`, `transaksi`, `detail_transaksi`, `pembayaran`, `cash_expense`, `cash_opname`, `jadwal`, `master_jadwal`, `master_jadwal_detail`, `bonus_rule`, serta `closing_kas`. Baseline juga membuat view `v_daftar_pembayaran` dan `v_pembayaran_item_harian`. Connection utama menggunakan MySQLi; connection testing menggunakan SQLite in-memory dan secara otomatis menjadi `defaultGroup` ketika `ENVIRONMENT === 'testing'`.                                                                                                                                           |
| Catatan            | Daftar migration aktif `v2.1`: `2026-09-08-000001_CreateAuliaPosCore.php`; `2026-09-09-000001_AddStatusMangkrakTransaksi.php`; `2026-09-09-000002_AddDiskonPelangganPersenTransaksi.php`; `2026-09-13-000001_AddPriorityToUsers.php`; `2026-09-14-000001_CreateClosingKasTable.php`. `CreateAuliaPosCore` membuat tabel parent/reference terlebih dahulu kemudian tabel yang memiliki foreign key, dan `down()` menghapus child sebelum parent. Migration `AddPriorityToUsers` juga membuat unique index `uq_users_priority`. `CreateClosingKasTable` membuat unique index tanggal untuk mencegah lebih dari satu closing pada tanggal yang sama.           |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.** Struktur migration, konfigurasi database, route/controller migration manual, command setup, dan dokumentasi terkait telah diperiksa.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

| Nama Subbab        | Testing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan struktur, jenis, konfigurasi, cara menjalankan, dan batasan pengujian AuliaPos agar perubahan kode dapat diverifikasi tanpa bergantung hanya pada pengujian manual.                                                                                                                                                                                                                                                                                                                                         |
| Deskripsi          | AuliaPos menggunakan **PHPUnit 10** sebagai framework testing melalui dependency `phpunit/phpunit:^10.5.16`. Command `composer test` menjalankan seluruh test suite. Test dibagi menjadi `tests/unit/` untuk logic murni, `tests/database/` untuk pengujian yang membutuhkan database, dan `tests/session/` untuk pengujian feature/HTTP dengan session. Test support khusus berada di `tests/_support/`.                                                                                                               |
| Aturan Bisnis      | Logic yang bersifat **pure/stateless** diutamakan diuji sebagai unit test tanpa bootstrap CodeIgniter penuh. Logic yang menyentuh database menggunakan `CIUnitTestCase` dan `DatabaseTestTrait`. Workflow HTTP yang melibatkan routing, authentication/session, Controller, Model, dan database diuji melalui feature/session test. Test harus memverifikasi behavior yang memang menjadi aturan aplikasi, bukan hanya memastikan method dapat dipanggil.                                                               |
| Alur               | Developer mengubah kode → menjalankan test yang relevan → jika perubahan memengaruhi banyak layer, menjalankan seluruh suite dengan `composer test` → PHPUnit menjalankan test di `tests/` → hasil test dan assertion digunakan untuk menentukan apakah behavior tetap sesuai. Test tertentu dapat dijalankan menggunakan path file atau `--filter`.                                                                                                                                                                    |
| Hak Akses          | Testing dijalankan dari environment development/test melalui CLI dan tidak merupakan endpoint operasional aplikasi. Database testing diarahkan ke connection `tests`. `app/Config/Database.php` secara eksplisit mengganti `defaultGroup` menjadi `tests` ketika `ENVIRONMENT === 'testing'`, sehingga test tidak menggunakan koneksi database utama secara tidak sengaja.                                                                                                                                              |
| Kondisi & Batasan  | Unit test tidak membutuhkan database jika objek yang diuji benar-benar stateless. Database dan session test membutuhkan bootstrap CodeIgniter serta database test. Konfigurasi `phpunit.dist.xml` menyediakan `testsuite` untuk seluruh folder `tests/`; konfigurasi database test di file tersebut masih berupa contoh/commented configuration, sedangkan `Database.php` menyediakan SQLite3 `:memory:` sebagai default connection `tests`. Coverage mengecualikan `app/Views` dan `app/Config/Routes.php`.            |
| Data yang Terlibat | Data input/output logic bisnis, database SQLite test `:memory:`, migration dan seed khusus di `tests/_support/`, session authentication untuk feature test, HTTP request/response, serta assertion PHPUnit. Contoh test mencakup kalkulasi status pembayaran, diskon transaksi, jatuh tempo, validasi tanggal closing kas, pembayaran, otorisasi Shift Leader, dan workflow penyelesaian transaksi.                                                                                                                     |
| Catatan            | Pola test yang sudah diterapkan antara lain `KalkulasiDiskonTransaksiTest`, `KalkulasiStatusPembayaranTest`, `KalkulasiJatuhTempoTest`, dan `ClosingKasValidasiTanggalTest` menggunakan `PHPUnit\Framework\TestCase` karena logic-nya pure/stateless. Test database menggunakan `CIUnitTestCase` + `DatabaseTestTrait`, sedangkan test session menggunakan `FeatureTestTrait` dan database test. `phpunit.dist.xml` juga dikonfigurasi untuk menghasilkan TestDox, JUnit log, Clover, HTML coverage, dan coverage text. |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

| Nama Subbab        | Logging & Error Handling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan bagaimana AuliaPos mencatat kejadian aplikasi, menangani exception/error, membedakan response ke user dari informasi teknis, dan melakukan fallback ketika proses tertentu gagal.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Deskripsi          | AuliaPos menggunakan mekanisme logging dan exception handling bawaan CodeIgniter 4. Konfigurasi utama berada di `app/Config/Logger.php` dan `app/Config/Exceptions.php`. `Logger` menggunakan `FileHandler` dengan default path `WRITEPATH/logs/`, sedangkan `Exceptions` mengaktifkan logging exception dan menggunakan `ExceptionHandler` bawaan CodeIgniter.                                                                                                                                                                                                                                                                                     |
| Aturan Bisnis      | Error pada backend tidak boleh dianggap selesai hanya karena UI dapat menampilkan pesan. Operasi penting menggunakan `try/catch` pada titik yang memang membutuhkan fallback atau response error terkontrol. Error teknis dicatat menggunakan `log_message()`, sementara response ke browser dapat menggunakan pesan yang lebih sesuai untuk user. Pada kasus tertentu seperti kegagalan perhitungan Shift Leader, aplikasi menerapkan **fail closed** dengan menganggap authority tidak tersedia daripada memberikan akses berdasarkan asumsi.                                                                                                     |
| Alur               | Terjadi error/exception → CodeIgniter atau kode aplikasi menangkapnya → jika perlu dicatat menggunakan `log_message()` → Controller dapat mengembalikan response error atau melakukan fallback → exception yang tidak ditangani diproses oleh `ExceptionHandler` CodeIgniter. Exception logging aktif secara default dan status `404` dikecualikan dari logging melalui `ignoreCodes`.                                                                                                                                                                                                                                                              |
| Hak Akses          | File log berada di area `writable`, bukan bagian dari UI operasional biasa. Informasi error internal tidak seharusnya dijadikan response browser secara langsung apabila tidak diperlukan. Logging digunakan oleh layer aplikasi seperti Controller dan Service untuk membantu diagnosis kegagalan.                                                                                                                                                                                                                                                                                                                                                 |
| Kondisi & Batasan  | `Logger::$threshold` pada source saat ini bernilai `9`, sehingga seluruh level logging yang dikonfigurasi `FileHandler` dapat dicatat. Komentar konfigurasi menyebutkan bahwa live site biasanya cukup menggunakan level `3` atau lebih tinggi, tetapi **nilai aktual source v2.1 adalah `9`**. `Exceptions::$log` bernilai `true`, sedangkan `404` berada dalam `ignoreCodes`. Belum terdapat custom exception handler aplikasi; `handler()` mengembalikan `CodeIgniter\Debug\ExceptionHandler`.                                                                                                                                                   |
| Data yang Terlibat | Message log, level `error`, `warning`, `info`, `debug`, dan level PSR-3 lainnya; exception message/trace; file log di `WRITEPATH/logs/`; HTTP response JSON/HTML; serta konteks operasi seperti transaksi, archive, Shift Leader, laporan, dan pencetakan.                                                                                                                                                                                                                                                                                                                                                                                          |
| Catatan            | Source menggunakan logging pada beberapa pola berbeda. `Authority` mencatat kegagalan menghitung Shift Leader sebagai `error` lalu mengembalikan `false`. `ArchiveTransaksi` mencatat keberhasilan sebagai `info` dan kegagalan sebagai `error`. `Cetak` menggunakan `warning` untuk masalah logo. `Api` mencatat error simpan transaksi beserta trace. `Laporan` menggunakan `debug` untuk diagnosis proses laporan. Beberapa Controller juga menangkap exception lalu mengembalikan JSON `status=error`. Karena log dapat berisi message dan trace exception, log tetap merupakan data teknis yang perlu diperlakukan sebagai informasi internal. |
| Status             | **Aktif — terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

| Nama Subbab        | Security                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan mekanisme keamanan yang sudah diterapkan pada AuliaPos, termasuk authentication, authorization, CSRF, session, password, input validation, upload file, dan perlindungan pada endpoint backend.                                                                                                                                                                                                                                                                                                                                                                                        |
| Deskripsi          | Security AuliaPos menggunakan kombinasi mekanisme bawaan CodeIgniter 4 dan aturan keamanan pada application layer. Authentication menggunakan session melalui `AuthFilter`, authorization menggunakan role `admin`/`kasir` serta pemeriksaan konteks seperti ownership transaksi dan Effective Shift Leader. Password disimpan menggunakan `password_hash()` dan diverifikasi dengan `password_verify()`. CodeIgniter juga menyediakan CSRF, invalid-character filter, secure headers, HTTPS filter, dan mekanisme keamanan lainnya, tetapi tidak semuanya aktif secara global pada source `v2.1`. |
| Aturan Bisnis      | Hak akses tidak boleh hanya bergantung pada UI. Backend harus memvalidasi kembali role, ownership (`kasir_id`), `sumber`, status transaksi/pembayaran, serta authority Shift Leader. Role database hanya `admin` dan `kasir`; Effective Shift Leader merupakan authority dinamis dan bukan role baru. Password tidak disimpan dalam bentuk plaintext. Upload foto profil divalidasi ukuran, MIME, dan isi gambar serta disimpan di luar `public/`.                                                                                                                                                 |
| Alur               | Request → route/filter authentication → Controller/API → validasi input dan authorization → Model/Service → database → response. Form HTML menggunakan `csrf_field()`. Endpoint sensitif menggunakan filter `auth`. Password login diverifikasi terhadap `password_hash`. Upload profil melewati validasi dan penyimpanan server-side.                                                                                                                                                                                                                                                             |
| Hak Akses          | `AuthFilter` melindungi route aplikasi tertentu dan memiliki pengecualian untuk login/assets. Route admin menggunakan prefix yang diperiksa oleh `AuthFilter`. Operasi tertentu membutuhkan admin, sedangkan beberapa workflow dapat menggunakan kasir pemilik transaksi atau Effective Shift Leader sesuai aturan bisnis.                                                                                                                                                                                                                                                                         |
| Kondisi & Batasan  | **CSRF alias tersedia tetapi tidak diaktifkan sebagai global filter** pada `app/Config/Filters.php`; `globals.before` untuk `csrf` masih dikomentari. Sebaliknya, form-form POST yang ditemukan menggunakan `csrf_field()`, sehingga perlindungan form bergantung pada penerapan token di form dan konfigurasi/filter yang benar-benar aktif. `forceGlobalSecureRequests=false`, `secureheaders` juga tidak aktif global, dan `CSPEnabled=false` pada `App.php`. Ini merupakan kondisi aktual v2.1, bukan asumsi bahwa seluruh hardening CodeIgniter otomatis aktif.                               |
| Data yang Terlibat | Session authentication, `users.role`, `users.is_active`, `users.priority`, `password_hash`, CSRF cookie/token, HTTP request, transaction ownership, transaction/payment status, uploaded profile image, database, serta file di `WRITEPATH`.                                                                                                                                                                                                                                                                                                                                                       |
| Catatan            | `docs/AULIA.md` sendiri mencatat beberapa area security/hardening yang masih teridentifikasi sebagai scope terpisah, antara lain whitelist metode pembayaran pada endpoint tertentu, validasi nilai cash/kembalian dari client, potensi injection pada inline `onclick`, CSRF, concurrency, dan audit log. CodeIgniter terus menerima security fixes; karena repository menggunakan constraint `^4.7`, versi dependency yang terpasang perlu diverifikasi saat deployment dan dipatch secara berkala.                                                                                              |
| Status             | **Aktif — mekanisme existing terverifikasi pada branch `v2.1`; hardening tambahan masih tercatat sebagai area perhatian.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

| Nama Subbab        | Performance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan mekanisme performance yang sudah tersedia pada AuliaPos dan pola implementasi yang memengaruhi beban database, request, session, dan pemrosesan data.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Deskripsi          | AuliaPos menggunakan CodeIgniter 4 dengan pendekatan MVC + Service Layer dan database MySQL/MariaDB sebagai database utama. `app/Config/Filters.php` memasang `PageCache` sebagai required filter sebelum dan sesudah request serta `PerformanceMetrics` sebagai after filter. Composer juga mengaktifkan `optimize-autoloader=true`. Untuk data lama, `TransaksiArchiveService` memindahkan transaksi lama ke SQLite archive sehingga query data aktif tidak harus selalu menggunakan seluruh histori transaksi. CodeIgniter sendiri dirancang sebagai framework dengan footprint kecil dan fokus pada performance. |
| Aturan Bisnis      | Performance tidak boleh mengubah sumber kebenaran data atau melewati validasi bisnis. Archive merupakan pemisahan data lama, bukan penghapusan tanpa verifikasi. Perhitungan status pembayaran, diskon, dan jatuh tempo yang pure/stateless dibuat agar dapat dijalankan dan diuji tanpa bootstrap database/framework penuh.                                                                                                                                                                                                                                                                                         |
| Alur               | Request → filter `PageCache`/application filter → Controller → Model/Service → database → response. Untuk transaksi lama, Controller dapat mencari data pada archive ketika diperlukan. Report tertentu meng-query database utama dan archive secara terpisah lalu menggabungkan hasil di PHP karena MySQL dan SQLite tidak melakukan cross-database join.                                                                                                                                                                                                                                                           |
| Hak Akses          | Mekanisme performance tidak memberikan bypass authorization. Cache/filter tetap berada di dalam lifecycle request. Archive transaksi dijalankan melalui workflow admin dan `TransaksiArchiveService`; akses data archive mengikuti authorization Controller yang memanggilnya.                                                                                                                                                                                                                                                                                                                                       |
| Kondisi & Batasan  | `PageCache` dan `PerformanceMetrics` memang terdaftar sebagai required filters, tetapi konfigurasi cache dan metrics perlu dipahami dari behavior CodeIgniter/runtime; dokumentasi ini tidak menganggap seluruh halaman otomatis memiliki cache yang efektif. Tidak ditemukan dedicated caching layer untuk business data seperti Redis. `CashBalanceService` menghitung saldo berdasarkan data database secara real-time dan tidak menggunakan running-ledger table. Beberapa laporan dapat melakukan query dan agregasi cukup besar.                                                                               |
| Data yang Terlibat | Request/response cache, performance metrics, database MySQL/MariaDB, database archive SQLite, transaksi aktif/archive, pembayaran, detail transaksi, laporan, serta filesystem `writable`.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Catatan            | `composer.json` menggunakan `optimize-autoloader=true` dan `preferred-install=dist`. `TransaksiArchiveService` menggunakan SQLite untuk histori lama dan query archive secara lokal; hasil archive dan live kemudian digabung pada application layer. `phpunit.dist.xml` juga mengaktifkan cache PHPUnit di `build/.phpunit.cache`. Tidak ada evidence pada v2.1 tentang profiling production, benchmark formal, Redis/Memcached, queue worker, atau asynchronous job system, sehingga hal-hal tersebut tidak didokumentasikan sebagai existing feature.                                                             |
| Status             | **Aktif — konfigurasi dan pola performance terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

| Nama Subbab        | Deployment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan kebutuhan environment, proses instalasi, konfigurasi runtime, database migration/seed, filesystem writable, dan langkah deployment AuliaPos ke server.                                                                                                                                                                                                                                                                                                                                                                                                    |
| Deskripsi          | AuliaPos adalah aplikasi CodeIgniter 4 dengan PHP `^8.2`, MySQL/MariaDB, Composer, dan web server yang diarahkan ke folder `public/`. Setup repository menggunakan `composer install`, membuat `.env` dari template `env`, mengisi `baseURL` dan `database.default.*`, menjalankan migration, menjalankan `AuliaPosInitialSeeder`, kemudian menjalankan test.                                                                                                                                                                                                         |
| Aturan Bisnis      | Deployment harus memisahkan configuration/secrets dari source code melalui `.env`. Database schema dipasang melalui migration, sedangkan data master/reference dipasang melalui `AuliaPosInitialSeeder`; operational data tidak di-seed. Migration yang sudah pernah dijalankan tidak diedit untuk deployment berikutnya; perubahan schema baru dibuat sebagai migration baru.                                                                                                                                                                                        |
| Alur               | Siapkan PHP dan extension → `composer install` → buat `.env` → isi `app.baseURL` dan `database.default.*` → pastikan folder `writable/` dapat ditulis → `php spark migrate` → `php spark db:seed AuliaPosInitialSeeder` → konfigurasi printer jika digunakan → jalankan `composer test` → arahkan web server ke `public/` → verifikasi login dan workflow utama. Untuk hosting tanpa CLI/SSH, repository menyediakan `/migrasi-manual` yang dibatasi admin dan meminta konfirmasi `JALANKAN`.                                                                         |
| Hak Akses          | Deployment normal membutuhkan akses server/CLI atau mekanisme deployment yang dapat menjalankan Composer dan Spark. Endpoint `/migrasi-manual` hanya untuk admin dan merupakan fallback ketika CLI tidak tersedia. Source Controller sendiri memberi peringatan bahwa endpoint migration manual sebaiknya tidak dibiarkan terbuka permanen di production.                                                                                                                                                                                                             |
| Kondisi & Batasan  | `App.php` memiliki default development `http://localhost/aulia/`, tetapi production seharusnya menggunakan `baseURL` yang sesuai environment. `forceGlobalSecureRequests=false` pada source sehingga HTTPS harus diperhatikan pada konfigurasi deployment/web server. Session default menggunakan `FileHandler` dengan storage di `WRITEPATH/session`. Archive menggunakan SQLite di `WRITEPATH/archive/aulia_pos_archive.db`. Konfigurasi printer dan executable SumatraPDF berada di server melalui `PrintNota.php`/`.env`; browser tidak mengontrol path tersebut. |
| Data yang Terlibat | Source code, `vendor/`, `.env`, database MySQL/MariaDB, migration history, master/reference data, `writable/session`, `writable/logs`, `writable/uploads`, SQLite archive, konfigurasi printer, dan filesystem web server.                                                                                                                                                                                                                                                                                                                                            |
| Catatan            | README mendefinisikan kebutuhan PHP 8.2+ beserta extension `intl`, `mbstring`, `json`, `mysqlnd`, dan `curl`. `composer.json` mengunci framework pada `^4.7`, Dompdf `^3.1`, dan escpos-php `^5.0`. Saat ini CodeIgniter 4.7.4 adalah rilis terbaru yang ditampilkan situs resmi, dan rilis 4.7.4 mencakup security/stability fixes; karena constraint repository adalah `^4.7`, dependency lock/version yang benar-benar terpasang perlu diverifikasi sebelum deployment, bukan diasumsikan dari `composer.json` saja.                                               |
| Status             | **Aktif — proses setup dan komponen deployment terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

| Nama Subbab        | Backup & Recovery                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan mekanisme backup yang sudah tersedia, perlindungan sebelum operasi database yang destruktif, archive transaksi, serta batas kemampuan recovery pada AuliaPos.                                                                                                                                                                                                                                                                                                                       |
| Deskripsi          | AuliaPos memiliki beberapa mekanisme backup yang berbeda. Maintenance import produk membuat backup tabel `produk` otomatis sebelum perubahan dijalankan. Archive transaksi membuat backup JSON mentah sebelum data disalin ke SQLite archive. Migration manual tidak membuat backup database secara otomatis dan secara eksplisit meminta operator melakukan backup terlebih dahulu. Backup database production penuh tetap merupakan tanggung jawab deployment/infrastruktur di luar aplikasi. |
| Aturan Bisnis      | Operasi destruktif tidak boleh langsung menghapus data tanpa tahap perlindungan dan validasi. Archive transaksi mengikuti urutan: pilih bulan → preview → konfirmasi `ARCHIVE` → backup JSON → salin ke SQLite → validasi jumlah baris dan total `grand_total` → baru hapus data MySQL. Jika validasi gagal, data MySQL tidak dihapus. Jika penghapusan MySQL gagal setelah archive tervalidasi, data tetap berada di kedua tempat sehingga proses dapat diulang.                               |
| Alur               | **Maintenance produk:** upload/import → preview dan validasi → backup tabel produk → validasi ulang terhadap DB terbaru → UPDATE/INSERT/DELETE dalam transaction. **Archive transaksi:** backup JSON → copy ke SQLite secara idempotent → validasi → delete dari MySQL. **Recovery:** archive tidak menyediakan tombol restore otomatis; pemulihan dilakukan manual dari SQLite archive atau backup JSON.                                                                                       |
| Hak Akses          | Maintenance import dan archive mengikuti authorization backend dan workflow admin yang sesuai. Migration manual hanya admin dan meminta konfirmasi eksplisit. Backup database production eksternal membutuhkan akses administrator server/database.                                                                                                                                                                                                                                             |
| Kondisi & Batasan  | Backup aplikasi bukan pengganti backup database production penuh. File backup JSON archive disimpan di `writable/archive/backups/` dan tidak memiliki cleanup otomatis. Tidak ada UI restore otomatis dari archive ke MySQL. Archive lebih tepat dipandang sebagai mekanisme archival + safety copy, bukan disaster-recovery system lengkap.                                                                                                                                                    |
| Data yang Terlibat | Tabel `produk`, file backup SQL untuk maintenance produk, transaksi lama, detail transaksi, pembayaran, SQLite archive `writable/archive/aulia_pos_archive.db`, backup JSON archive, database MySQL/MariaDB utama, dan file migration.                                                                                                                                                                                                                                                          |
| Catatan            | Backup maintenance produk sengaja tidak bergantung pada executable `mysqldump`; backup dibuat melalui koneksi database aplikasi sehingga lebih portable, termasuk environment XAMPP. `docs/AULIA.md` juga secara eksplisit menyarankan backup database utama secara berkala di luar fitur archive.                                                                                                                                                                                              |
| Status             | **Aktif — mekanisme backup/archive terverifikasi pada branch `v2.1`; disaster recovery penuh belum menjadi fitur aplikasi.**                                                                                                                                                                                                                                                                                                                                                                    |

| Nama Subbab        | Development Workflow                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan alur kerja developer AuliaPos dari memahami requirement dan dokumentasi, melakukan perubahan source code, menjalankan test, sampai memastikan perubahan tetap sesuai aturan bisnis.                                                                                                                                                                                                                                                                                                                                                                                                    |
| Deskripsi          | Workflow development AuliaPos berpusat pada source code branch yang sedang dikerjakan, dokumentasi aktif, dan test suite. `docs/README.md` menjadi peta dokumentasi. `docs/AULIA.md` merupakan sumber kebenaran aturan bisnis POS, `docs/USER-SHIFT.md` untuk Priority/Effective Shift Leader, dan `docs/CHANGELOG.md` mencatat alasan perubahan. `docs/archive/` dibekukan dan bukan sumber kebenaran aktif.                                                                                                                                                                                      |
| Aturan Bisnis      | Sebelum mengubah lifecycle transaksi, pembayaran, kas, tagihan, reporting, atau aturan Shift Leader, developer harus membaca dokumentasi domain yang relevan. Perubahan status transaksi harus melalui `TransaksiModel::ubahStatus()`. Penulisan pembayaran harus melalui `TransaksiModel::tambahPembayaran()`. Developer tidak boleh membuat aturan lifecycle/payment baru hanya di Controller/API.                                                                                                                                                                                               |
| Alur               | Baca `README.md` dan `docs/README.md` → baca dokumentasi domain terkait → pahami source code yang terkena dampak → lakukan perubahan pada layer yang sesuai → update test jika behavior berubah → jalankan test yang relevan → jalankan full `composer test` → untuk perubahan schema gunakan migration baru → untuk data master gunakan seeder → verifikasi hasil. Command utama yang terdokumentasi adalah `composer test`, PHPUnit individual/filter, `php spark migrate`, `php spark migrate:rollback`, `php spark db:seed AuliaPosInitialSeeder`, dan `php spark aulia:repair-total-dibayar`. |
| Hak Akses          | Development workflow menggunakan Git branch dan akses repository. Operasi database production seperti migration manual dibatasi admin. Test database menggunakan konfigurasi test, bukan database operasional.                                                                                                                                                                                                                                                                                                                                                                                     |
| Kondisi & Batasan  | `v2.1` adalah baseline tanpa Chat. Dokumentasi `v2.2` tidak menjadi sumber implementasi untuk branch ini. `docs/archive/` tidak boleh diedit. Tidak semua business logic berada di Service; sebagian tetap berada di Model dan Controller/API sesuai implementasi aktual. Untuk perubahan besar yang mengubah behavior lintas modul, impact terhadap business-rules dan test harus diperiksa sebelum dianggap selesai.                                                                                                                                                                             |
| Data yang Terlibat | Source `app/`, tests `tests/`, migration/seeder, `docs/`, `.env`, Composer dependencies, database development/test, serta Git branch dan commit history.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Catatan            | `CLAUDE.md` secara eksplisit menyediakan command untuk full test, test individual, migration, seeder, dan repair cache `total_dibayar`. Untuk hosting tanpa CLI tersedia `/migrasi-manual`, tetapi route tersebut dimaksudkan sebagai fallback dan bukan pengganti workflow CLI normal. Perubahan frontend kasir menggunakan shared JavaScript `kasir-shared.js` dan `payment.js`; perubahan pada area shared harus memperhatikan penggunaan oleh `kasir/index.php` dan `kasir/edit.php`.                                                                                                          |
| Status             | **Aktif — workflow dan aturan development terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

| Nama Subbab        | Coding Convention                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan pola penamaan, struktur file, pembagian layer, gaya komentar, dan pola implementasi yang digunakan source code AuliaPos agar perubahan baru konsisten dengan codebase.                                                                                                                                                                                                                                                                                                                                         |
| Deskripsi          | AuliaPos mengikuti struktur CodeIgniter 4 dan PSR-4: namespace `App\` diarahkan ke `app/`, sedangkan `Config\` diarahkan ke `app/Config/`. Class PHP menggunakan nama PascalCase seperti `TransaksiModel`, `KalkulasiDiskonTransaksi`, dan `EffectiveShiftLeaderService`. Method menggunakan camelCase seperti `tambahPembayaran()`, `ubahStatus()`, dan `shiftLeaderSaatIni()`. Nama tabel dan kolom database mengikuti snake_case seperti `transaksi_id`, `kasir_id`, `status_pembayaran`, dan `created_at`.             |
| Aturan Bisnis      | Penamaan harus mempertahankan vocabulary domain Indonesia yang sudah digunakan aplikasi. Jangan mengubah istilah domain hanya untuk mengikuti naming convention bahasa Inggris jika perubahan tersebut membuat kontrak database, route, atau business rule tidak konsisten. Layer harus tetap mengikuti tanggung jawab aktual: Controller untuk request/workflow, Model untuk data/domain rule tertentu, Service untuk logic yang memang dipisahkan, View untuk presentation, dan `Api.php` sebagai Controller JSON/AJAX.  |
| Alur               | Tentukan layer dan lokasi file → gunakan namespace sesuai struktur `App\...` → gunakan nama class/method mengikuti pola existing → gunakan field database sesuai schema → tambahkan komentar jika terdapat aturan atau keputusan yang tidak obvious → tambahkan/update test jika behavior berubah → jalankan formatter/linter jika tersedia dalam workflow project.                                                                                                                                                        |
| Hak Akses          | Convention tidak menentukan authorization. Authorization tetap dilakukan melalui filter, Controller/API, Model/Service, ownership, role, dan authority sesuai kebutuhan operasi. Jangan menganggap nama method atau lokasi file sebagai security boundary.                                                                                                                                                                                                                                                                 |
| Kondisi & Batasan  | Tidak ditemukan konfigurasi dedicated PHP-CS-Fixer, PHP_CodeSniffer, Pint, ESLint, atau formatter wajib di `composer.json`. Karena itu, dokumentasi ini tidak boleh mengklaim adanya automated style enforcement. Source juga belum sepenuhnya seragam secara stylistic; beberapa View masih menggunakan inline `<style>` dan inline JavaScript, sehingga convention harus mengikuti pola domain yang sudah ada tanpa menganggap seluruh repository telah direfactor menjadi satu style yang ketat.                        |
| Data yang Terlibat | PHP source, Controller, Model, Service, Filter, Config, View, JavaScript pada `public/assets/js/`, database schema, route URI, dan dokumentasi domain.                                                                                                                                                                                                                                                                                                                                                                     |
| Catatan            | Komentar business-rule dan dokumentasi domain menggunakan bahasa Indonesia dan `CLAUDE.md` meminta developer mempertahankan bahasa tersebut saat mengedit. Views menggunakan folder lowercase seperti `kasir`, `produk`, `pelanggan`, dan `transaksi`. File JavaScript bersama kasir berada di `public/assets/js/`, khususnya `kasir-shared.js` dan `payment.js`. `Routing.php` menunjukkan auto-routing dimatikan (`autoRoute=false`), sehingga route baru harus ditambahkan secara eksplisit di `app/Config/Routes.php`. |
| Status             | **Aktif — pola naming dan struktur terverifikasi pada branch `v2.1`; belum ada automated coding-style enforcement.**                                                                                                                                                                                                                                                                                                                                                                                                       |

| Nama Subbab        | Git & Branching                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menjelaskan struktur branch AuliaPos, fungsi masing-masing branch, serta aturan penggunaan branch agar perubahan tidak mencampur baseline POS dengan fitur yang belum menjadi bagian branch tersebut.                                                                                                                                                                                                                                            |
| Deskripsi          | AuliaPos menggunakan Git untuk memisahkan baseline dan pengembangan fitur. Pada baseline yang didokumentasikan, `v2.1` merupakan POS inti tanpa modul Chat. `v2.2` merupakan `v2.1` ditambah Shared WhatsApp Inbox. `v2.x` merepresentasikan POS inti lama. Repository juga memiliki tag archive untuk versi lama.                                                                                                                               |
| Aturan Bisnis      | Perubahan harus dikerjakan pada branch yang sesuai dengan scope fitur. `v2.1` tidak boleh dianggap memiliki fitur Chat/Shared WhatsApp Inbox. Dokumentasi dan source harus dibaca berdasarkan branch yang sedang dikerjakan. Perubahan schema database harus menggunakan migration baru, bukan mengubah migration yang sudah pernah dieksekusi pada environment lain.                                                                            |
| Alur               | Pastikan branch aktif → pahami baseline branch → baca dokumentasi dan source yang terkait → lakukan perubahan → jalankan test yang relevan → jalankan `composer test` → review perubahan → commit perubahan ke branch yang sesuai. Untuk perubahan database, buat migration baru kemudian jalankan `php spark migrate` pada environment target. CodeIgniter memang menggunakan tabel `migrations` untuk melacak migration yang sudah dijalankan. |
| Hak Akses          | Git branch dan repository mengikuti akses developer/repository. Hak akses aplikasi seperti admin/kasir merupakan concern berbeda dan tidak ditentukan oleh Git branch.                                                                                                                                                                                                                                                                           |
| Kondisi & Batasan  | Tidak ditemukan aturan repository yang menetapkan Git Flow, trunk-based development, format commit message wajib, atau kebijakan pull request tertentu. Karena itu, dokumentasi tidak boleh mengklaim workflow tersebut sebagai aturan resmi project. Branch yang terdokumentasi secara eksplisit adalah `v2.x`, `v2.1`, dan `v2.2`.                                                                                                             |
| Data yang Terlibat | Git repository, branch, commit, source code, `docs/`, migration, tests, `composer.lock`, dan konfigurasi environment. `composer.lock` menjaga dependency project pada state yang diketahui saat dependency di-install.                                                                                                                                                                                                                           |
| Catatan            | `CLAUDE.md` menjadi panduan kerja developer/AI di repository. `docs/README.md` menentukan dokumen aktif sebagai sumber kebenaran dan menyatakan `docs/archive/` dibekukan. Git sendiri mendukung branch sebagai unit pengembangan terpisah dan berbagai workflow branching; AuliaPos hanya perlu mengikuti aturan branch yang benar-benar terdokumentasi di repository.                                                                          |
| Status             | **Aktif — struktur branch dan aturan terkait terverifikasi pada branch `v2.1`.**                                                                                                                                                                                                                                                                                                                                                                 |

| Nama Subbab        | Troubleshooting                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjelaskan pendekatan diagnosis ketika AuliaPos mengalami error pada aplikasi, database, routing, authentication, testing, migration, atau dependency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Deskripsi          | Troubleshooting dilakukan dari lapisan paling dekat dengan gejala menuju sumber masalah: environment/configuration → web server/routing → authentication/filter → Controller/API → Model/Service → database → filesystem/dependency. Informasi utama berasal dari error response saat development, log aplikasi, test output, dan kondisi konfigurasi. CodeIgniter menyediakan logging dan fasilitas debugging untuk membantu proses diagnosis.                                                                                                                                                                                                         |
| Aturan Bisnis      | Jangan memperbaiki gejala dengan melewati business rule atau security check. Jika masalah menyangkut status transaksi, gunakan jalur `TransaksiModel::ubahStatus()`. Jika menyangkut pembayaran, gunakan `TransaksiModel::tambahPembayaran()`. Jika masalah berasal dari migration, periksa status migration sebelum melakukan perubahan database manual.                                                                                                                                                                                                                                                                                               |
| Alur               | Identifikasi error → catat URL/action dan input yang relevan → periksa log `writable/logs/` → periksa konfigurasi `.env` dan `app/Config/` → periksa route/filter jika request gagal masuk → periksa Controller/API → periksa Model/Service → periksa database/schema → jalankan test terkait → jalankan full `composer test` setelah perbaikan. CodeIgniter mendokumentasikan log harian pada `writable/logs` dan menyediakan logging SQL untuk diagnosis database.                                                                                                                                                                                    |
| Hak Akses          | Diagnosis production membutuhkan akses server/log/database sesuai kebutuhan. Error detail tidak boleh dibuka kepada pengguna production karena dapat mengekspos informasi internal atau credential. CodeIgniter membedakan perilaku error development/testing dan production.                                                                                                                                                                                                                                                                                                                                                                           |
| Kondisi & Batasan  | Pada production, halaman `Whoops!` tidak memberikan detail error; informasi teknis harus dicari di log. Masalah yang hanya terjadi di server juga perlu memeriksa case-sensitivity nama file/folder, konfigurasi rewrite Apache, PHP extension, permission `writable/`, dan `.env`. CodeIgniter secara khusus mencatat bahwa perbedaan case filesystem dapat menyebabkan aplikasi bekerja di Windows tetapi gagal pada server case-sensitive.                                                                                                                                                                                                           |
| Data yang Terlibat | `writable/logs/`, `.env`, `app/Config/`, `app/Config/Logger.php`, `app/Config/Exceptions.php`, route/filter, database connection, migration history, PHPUnit output, Composer dependency, dan filesystem `writable/`.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Catatan            | Beberapa diagnosis umum: **404** → periksa `Routes.php`, URL, Apache rewrite dan `public/`; **database error** → periksa `.env`, koneksi, schema dan migration; **login/auth gagal** → periksa session, `AuthFilter`, role dan timeout; **test gagal** → jalankan test individual dengan PHPUnit lalu full suite; **migration gagal** → periksa migration history dan backup sebelum operasi production; **error hanya production** → periksa `writable/logs/` dan jangan mengaktifkan detail error secara sembarangan. CodeIgniter menyediakan dokumentasi troubleshooting khusus untuk rewrite, production error, case-sensitive filesystem, dan log. |
| Status             | **Aktif — pola troubleshooting terverifikasi dari source v2.1 dan dokumentasi CodeIgniter; belum ada runbook troubleshooting project yang lebih formal.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

| Nama Subbab        | Technical Decisions / ADR                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menjelaskan bagaimana keputusan teknis penting AuliaPos dicatat, dilacak, dan digunakan sebagai referensi ketika developer perlu memahami alasan di balik implementasi tertentu.                                                                                                                                                                                                                                                           |
| Deskripsi          | AuliaPos menggunakan dokumentasi keputusan teknis untuk mencatat alasan perubahan yang tidak cukup dijelaskan oleh source code saja. Repository memiliki folder `docs/decisions/` dan juga ADR eksplisit seperti `docs/adr/0001-reuse-response-state-for-queue-view-status.md`. `docs/CHANGELOG.md` digunakan untuk mencatat perubahan dan alasan historis, sedangkan dokumen business rules aktif menjadi sumber kebenaran aturan domain. |
| Aturan Bisnis      | Keputusan teknis yang mengubah behavior penting tidak boleh hanya tersimpan sebagai asumsi developer. Jika keputusan memengaruhi lifecycle transaksi, pembayaran, authorization, schema, architecture, atau integrasi, alasan dan konsekuensinya perlu dapat ditelusuri. Dokumen archive tidak boleh digunakan sebagai sumber keputusan aktif.                                                                                             |
| Alur               | Identifikasi masalah/keputusan → kumpulkan fakta dari source dan dokumentasi → catat alternatif dan alasan pemilihan bila relevan → simpan keputusan di `docs/decisions/` atau ADR yang sesuai → implementasikan perubahan → update test/dokumentasi yang terdampak → catat perubahan historis di `docs/CHANGELOG.md` bila diperlukan.                                                                                                     |
| Hak Akses          | ADR dan decision records merupakan dokumentasi developer; tidak menjadi mekanisme authorization aplikasi. Akses edit mengikuti repository/documentation workflow.                                                                                                                                                                                                                                                                          |
| Kondisi & Batasan  | Repository saat ini memiliki beberapa bentuk decision record, sehingga belum seluruhnya menggunakan satu template ADR yang seragam. `docs/README.md` menetapkan dokumen aktif sebagai source of truth, sedangkan `docs/archive/` dibekukan untuk penelusuran historis. Karena itu, dokumen historis tidak boleh diedit hanya untuk menyesuaikannya dengan keputusan saat ini.                                                              |
| Data yang Terlibat | Decision records, ADR, `docs/CHANGELOG.md`, `docs/AULIA.md`, `docs/USER-SHIFT.md`, source code, migration, tests, dan commit history.                                                                                                                                                                                                                                                                                                      |
| Catatan            | Salah satu keputusan arsitektural yang terdokumentasi adalah penggunaan satu chokepoint untuk lifecycle transaksi melalui `TransaksiModel::ubahStatus()` dan funnel pembayaran melalui `TransaksiModel::tambahPembayaran()`. Keputusan tersebut penting karena Controller/API tidak boleh membuat aturan lifecycle sendiri. ADR/decision record sebaiknya menjawab minimal: **masalah apa, keputusan apa, alasan, dampak, dan batasan**.   |
| Status             | **Aktif — mekanisme decision/ADR terverifikasi pada branch `v2.1`; format ADR belum sepenuhnya distandardisasi.**                                                                                                                                                                                                                                                                                                                          |
