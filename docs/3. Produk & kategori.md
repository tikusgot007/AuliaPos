| Nama Subbab        | Daftar Produk                                                                                                                                                                                                                                                                                            |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menyediakan daftar master produk yang aktif untuk dikelola admin, termasuk pencarian, pengurutan, pagination DataTables, edit, nonaktifkan, dan akses ke maintenance master barang.                                                                                                                      |
| Deskripsi          | `Produk::index()` mengambil produk aktif (`is_active = 1`) dengan informasi kategori, barcode, nama, satuan, harga jual, dan harga beli. Daftar utama juga memiliki endpoint DataTables server-side `/produk/get-produk-data`. Pencarian DataTables mencakup nama produk, barcode, kategori, dan satuan. |
| Aturan Bisnis      | Produk yang tampil pada daftar utama hanya produk aktif. Data produk memiliki relasi ke kategori. Master Produk dan seluruh endpoint mutasinya hanya dapat digunakan admin; kasir yang mengakses langsung akan diarahkan ke `/kasir`.                                                                    |
| Alur               | Admin membuka `/produk` → controller mengambil produk aktif dan kategori induk → view menampilkan daftar → DataTables dapat meminta `/produk/get-produk-data` untuk search/order/pagination → admin dapat memilih Tambah, Edit, Hapus/Nonaktifkan, atau Maintenance.                                     |
| Hak Akses          | **Admin:** akses penuh. **Kasir:** tidak dapat mengakses master Produk. **Guest:** diarahkan ke login. Proteksi diuji melalui `ProdukKategoriAdminOnlyTest`.                                                                                                                                             |
| Kondisi & Batasan  | Daftar utama tidak menampilkan produk nonaktif. Endpoint DataTables juga hanya mengambil produk aktif. Pengurutan DataTables dapat mengikuti parameter kolom/order yang dikirim client; tanpa order eksplisit model menggunakan `produk.id DESC`.                                                        |
| Data yang Terlibat | `produk`: `id`, `barcode`, `nama`, `kategori_id`, `satuan`, `harga_jual`, `harga_beli`, `is_active`, `is_locked`; `kategori.nama` untuk nama kategori.                                                                                                                                                   |
| Catatan            | Model juga menyediakan `getProdukAktif()` untuk POS, yang berbeda konteks dengan DataTables master. Produk aktif di POS diurutkan nama A–Z.                                                                                                                                                              |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                                                                                                             |

| Nama Subbab        | Tambah Produk                                                                                                                                                                                                                                           |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menambahkan produk baru ke master Produk sehingga dapat digunakan pada proses POS dan fitur lain yang membutuhkan katalog produk.                                                                                                                       |
| Deskripsi          | Form `/produk/tambah` menyediakan data kategori induk melalui `KategoriModel::getKategoriInduk()`. `Produk::simpan()` menerima nama, kategori, satuan, harga jual, harga beli, dan barcode opsional. Produk baru selalu dibuat aktif (`is_active = 1`). |
| Aturan Bisnis      | Nama wajib diisi minimal 3 karakter. `kategori_id` wajib berupa integer. `harga_jual` wajib numerik. Satuan default `pcs` bila tidak dikirim. Harga beli default `0`. Barcode hanya disimpan jika tidak kosong.                                         |
| Alur               | Admin membuka `/produk/tambah` → memilih kategori dan mengisi data → POST `/produk/simpan` → validasi server → `ProdukModel::save()` → produk dibuat aktif → kembali ke `/produk`. Jika validasi gagal, form dikembalikan bersama input dan error.      |
| Hak Akses          | Hanya **admin**. Route memakai `auth`, dan `AuthFilter` memperlakukan prefix `produk` sebagai area admin-only.                                                                                                                                          |
| Kondisi & Batasan  | Validasi controller hanya mensyaratkan nama, kategori, dan harga jual. Barcode tidak wajib. Tidak ada proses stok pada method `simpan()` ini.                                                                                                           |
| Data yang Terlibat | `produk.nama`, `produk.kategori_id`, `produk.satuan`, `produk.harga_jual`, `produk.harga_beli`, `produk.barcode`, `produk.is_active`.                                                                                                                   |
| Catatan            | Dropdown pada controller menggunakan kategori induk (`parent_id IS NULL`), bukan seluruh hirarki kategori.                                                                                                                                              |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                                                            |

| Nama Subbab        | Edit Produk                                                                                                                                                                                                      |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Mengubah informasi master produk yang sudah ada tanpa membuat produk baru.                                                                                                                                       |
| Deskripsi          | `/produk/edit/{id}` mengambil produk berdasarkan ID dan menampilkan form edit bersama daftar kategori induk. POST `/produk/update/{id}` memperbarui nama, kategori, satuan, harga jual, harga beli, dan barcode. |
| Aturan Bisnis      | Nama minimal 3 karakter, kategori wajib integer, dan harga jual wajib numerik. Satuan default `pcs`; harga beli default `0`. Barcode kosong disimpan sebagai `NULL`.                                             |
| Alur               | Admin memilih Edit → controller mencari produk → jika tidak ditemukan kembali ke daftar dengan error → jika ditemukan form ditampilkan → submit → validasi → `ProdukModel::update()` → kembali ke `/produk`.     |
| Hak Akses          | Hanya **admin**. Endpoint GET edit dan POST update termasuk daftar URL yang diuji sebagai admin-only.                                                                                                            |
| Kondisi & Batasan  | Form edit mengambil produk berdasarkan ID dan tidak secara eksplisit membatasi hasil `find()` hanya produk aktif. Namun daftar master hanya menampilkan produk aktif.                                            |
| Data yang Terlibat | `produk.id`, `barcode`, `nama`, `kategori_id`, `satuan`, `harga_jual`, `harga_beli`.                                                                                                                             |
| Catatan            | Selain form edit penuh, tersedia `update-inline` untuk perubahan field tertentu langsung dari daftar/data spreadsheet.                                                                                           |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                     |

| Nama Subbab        | Hapus Produk                                                                                                                                                                                                                                                                                              |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Mengeluarkan produk dari katalog aktif tanpa merusak histori transaksi.                                                                                                                                                                                                                                   |
| Deskripsi          | Aksi `/produk/hapus/{id}` pada CRUD biasa **bukan hard delete**. Controller mengubah `is_active` menjadi `0`, sehingga produk menjadi nonaktif dan tidak muncul pada daftar aktif/POS. Untuk kebutuhan pembersihan permanen tersedia mekanisme HAPUS melalui Maintenance Produk dengan validasi tambahan. |
| Aturan Bisnis      | Hapus dari CRUD biasa berarti **nonaktifkan**. Histori transaksi tidak dihapus. Hard delete melalui Maintenance hanya diperbolehkan jika produk bukan ID khusus `[1,2,4]`, tidak terkunci (`is_locked = 0`), dan belum pernah dipakai pada `detail_transaksi`.                                            |
| Alur               | CRUD biasa: admin pilih Hapus → `/produk/hapus/{id}` → `is_active = 0` → kembali ke daftar. Maintenance: CSV diberi `Aksi=HAPUS` → preview → validasi → backup → pengecekan ulang `bolehDihapus()` → DELETE bila memenuhi syarat.                                                                         |
| Hak Akses          | Hanya **admin**. Endpoint hapus termasuk URL yang diuji dalam `ProdukKategoriAdminOnlyTest`.                                                                                                                                                                                                              |
| Kondisi & Batasan  | Produk ID 1, 2, dan 4 merupakan ID khusus untuk Banner, Manual, dan Custom dan tidak boleh dihapus/dipakai ulang. Produk yang pernah memiliki detail transaksi juga tidak boleh hard delete, termasuk jika transaksi tersebut kemudian batal.                                                             |
| Data yang Terlibat | `produk.id`, `produk.is_active`, `produk.is_locked`, `detail_transaksi.produk_id`.                                                                                                                                                                                                                        |
| Catatan            | Istilah “hapus” pada CRUD dan “HAPUS” pada Maintenance memiliki efek berbeda: CRUD = soft disable, Maintenance = hard delete dengan guard.                                                                                                                                                                |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                                                                                                              |

| Nama Subbab        | Kategori Produk                                                                                                                                                                                                            |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Mengelompokkan produk menggunakan struktur kategori induk dan subkategori.                                                                                                                                                 |
| Deskripsi          | Modul `/kategori` mengelola daftar kategori, kategori induk, subkategori, penambahan, perubahan, dan penghapusan. `KategoriModel` menggunakan `parent_id` untuk membentuk hierarki.                                        |
| Aturan Bisnis      | Nama kategori wajib minimal 2 karakter dan unik. `parent_id` dapat `NULL` untuk kategori induk atau menunjuk kategori induk. Saat edit, kategori yang sedang diedit tidak boleh dipilih sebagai induk.                     |
| Alur               | Admin membuka `/kategori` → sistem mengambil seluruh kategori dan kategori induk → admin tambah/edit → validasi nama → simpan/update. Saat hapus, sistem terlebih dahulu memeriksa subkategori dan penggunaan oleh produk. |
| Hak Akses          | Hanya **admin**. Seluruh route CRUD kategori dilindungi `AuthFilter`.                                                                                                                                                      |
| Kondisi & Batasan  | Kategori yang masih mempunyai subkategori tidak dapat dihapus. Kategori yang masih digunakan oleh produk juga tidak dapat dihapus; produk harus dipindahkan terlebih dahulu.                                               |
| Data yang Terlibat | `kategori.id`, `kategori.nama`, `kategori.parent_id`, `created_at`, `updated_at`, serta `produk.kategori_id`.                                                                                                              |
| Catatan            | `getKategoriInduk()` mengambil kategori dengan `parent_id IS NULL`. Seeder mendefinisikan empat kategori utama pada master awal.                                                                                           |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                               |

| Nama Subbab        | Ukuran Produk                                                                                                                                                                                                                                                                                    |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menyediakan alat bantu perhitungan skala ukuran dan estimasi harga berdasarkan master produk Foto/Studio yang memiliki dimensi.                                                                                                                                                                  |
| Deskripsi          | Modul `/ukuran` mengambil produk aktif kategori ID `16` yang memiliki `panjang` dan `lebar` bernilai positif. Halaman menampilkan ukuran, harga, perhitungan skala, serta input ukuran custom.                                                                                                   |
| Aturan Bisnis      | Hanya produk kategori `16` yang aktif dan memiliki dimensi positif yang digunakan sebagai referensi. Perhitungan custom menggunakan luas `lebar × panjang`, mencari hingga tiga produk referensi terdekat, menghitung rata-rata harga per mm², kemudian membulatkan harga ke kelipatan tertentu. |
| Alur               | User membuka `/ukuran` → server mengambil produk Foto/Studio berdimensi → halaman menampilkan data → user memasukkan skala/ukuran custom → JavaScript menghitung ukuran hasil skala dan estimasi harga → `/ukuran/getList` menyediakan data harga untuk kalkulasi AJAX.                          |
| Hak Akses          | Semua **user yang login**, karena route hanya menggunakan `auth`; tidak termasuk prefix admin-only.                                                                                                                                                                                              |
| Kondisi & Batasan  | Produk tanpa `panjang`, `lebar`, atau dengan nilai `<= 0` tidak menjadi referensi. Nilai skala default UI adalah `2500`. Modul ini merupakan alat bantu perhitungan dan tidak menyimpan perubahan ukuran ke master produk.                                                                       |
| Data yang Terlibat | `produk.id`, `nama`, `kategori_id`, `panjang`, `lebar`, `harga_jual`, `is_active`, `sort_order`.                                                                                                                                                                                                 |
| Catatan            | Modul ini membaca dimensi dari tabel `produk`; bukan master tabel ukuran terpisah. Sebagian field dimensi merupakan bagian dari struktur produk/seeder yang dipakai oleh modul ini.                                                                                                              |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                                                                                                     |

| Nama Subbab        | Maintenance Produk                                                                                                                                                                                                                                                                                                |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Melakukan pembersihan dan koreksi master produk dalam jumlah besar dengan tetap mempertahankan kontrol terhadap histori transaksi dan menyediakan backup sebelum perubahan massal.                                                                                                                                |
| Deskripsi          | Maintenance menyediakan workflow CSV: export audit → edit CSV → preview/validasi → konfirmasi → backup otomatis → eksekusi perubahan. Aksi yang didukung adalah `PERTAHANKAN`, `NONAKTIF`, `HAPUS`, dan `INSERT`.                                                                                                 |
| Aturan Bisnis      | Preview tidak mengubah database. Saat eksekusi, CSV divalidasi ulang karena database dapat berubah setelah preview. Backup tabel `produk` wajib berhasil sebelum perubahan dilakukan. Baris `blocked` atau `error` tidak dieksekusi.                                                                              |
| Alur               | Admin membuka Maintenance → download CSV audit → mengubah kolom `Aksi` → upload CSV → `previewImport()` membaca dan memvalidasi → sistem menampilkan ringkasan → admin konfirmasi → `eksekusiImport()` validasi ulang → backup tabel → transaksi DB menjalankan UPDATE/NONAKTIF/HAPUS/INSERT → hasil ditampilkan. |
| Hak Akses          | Hanya **admin**. Controller melakukan pemeriksaan role sendiri selain route/auth filter.                                                                                                                                                                                                                          |
| Kondisi & Batasan  | `HAPUS` tetap tunduk pada `bolehDihapus()`: ID khusus, produk terkunci, atau produk yang pernah dipakai akan diblokir. File sementara disimpan di `WRITEPATH/uploads/maintenance`; token dipakai untuk mengidentifikasi file hasil preview.                                                                       |
| Data yang Terlibat | Seluruh field master `produk`, statistik `detail_transaksi`, serta tanggal transaksi pertama/terakhir. CSV memiliki kolom `ID`, `Nama`, `Kategori_ID`, `Kategori`, `Satuan`, `Harga_Jual`, `Harga_Beli`, `Aktif`, `Locked`, statistik penggunaan, dan `Aksi`.                                                     |
| Catatan            | Backup dibuat sebagai file SQL berisi INSERT seluruh tabel `produk`, bukan bergantung pada binary `mysqldump`. Eksekusi perubahan berada dalam transaksi database setelah backup berhasil.                                                                                                                        |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                                                                                                                      |

| Nama Subbab        | Import Produk                                                                                                                                                                                                                                                                                 |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Memasukkan perubahan atau produk baru secara massal melalui CSV dengan mekanisme preview dan validasi sebelum database diubah.                                                                                                                                                                |
| Deskripsi          | Import merupakan bagian dari Maintenance Produk. Endpoint `/produk/preview-import` menangani upload dan validasi awal, sedangkan `/produk/eksekusi-import` menjalankan perubahan setelah konfirmasi.                                                                                          |
| Aturan Bisnis      | File harus diproses melalui tahap preview terlebih dahulu. Data divalidasi terhadap kondisi database saat ini. `INSERT` digunakan untuk baris baru tanpa ID. `UPDATE`/`NONAKTIF` mengubah produk yang sudah ada. `HAPUS` hanya dilakukan bila lolos seluruh pemeriksaan keamanan penghapusan. |
| Alur               | Upload CSV → file dipindahkan ke storage sementara → `validasiCsv()` menghasilkan ringkasan dan status setiap baris → admin memeriksa preview → sistem membuat backup → CSV divalidasi ulang → perubahan dijalankan dalam transaksi DB → hasil import dikembalikan.                           |
| Hak Akses          | Hanya **admin**. Kedua endpoint import secara eksplisit melakukan pengecekan `session()->get('role') == 'admin'`.                                                                                                                                                                             |
| Kondisi & Batasan  | Preview tidak melakukan INSERT/UPDATE/DELETE. File hasil preview harus masih tersedia saat eksekusi. Token dibersihkan dengan `basename()` untuk mencegah path traversal. Baris error/blocked dilewati.                                                                                       |
| Data yang Terlibat | CSV master produk, tabel `produk`, `kategori`, `detail_transaksi`, serta backup tabel `produk`.                                                                                                                                                                                               |
| Catatan            | Import bukan endpoint generik untuk semua master data; implementasinya spesifik untuk Maintenance Master Barang.                                                                                                                                                                              |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                                                                                                  |

| Nama Subbab        | Export Produk                                                                                                                                                                                                                                          |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Mengekspor data master produk beserta statistik penggunaan sebagai bahan audit, pembersihan, dan maintenance.                                                                                                                                          |
| Deskripsi          | `/produk/export-audit` menjalankan `Produk::exportAudit()`, mengambil data melalui `ProdukModel::getAuditData()`, lalu menghasilkan CSV `Audit_Produk_<timestamp>.csv`. Data mencakup master produk dan statistik pemakaiannya pada histori transaksi. |
| Aturan Bisnis      | Export audit mencakup produk yang aktif maupun nonaktif karena query audit tidak memberikan filter `is_active`. Kolom `Aksi` sengaja dikosongkan agar admin dapat mengisinya sebelum melakukan import maintenance.                                     |
| Alur               | Admin memilih Download CSV Audit → controller mengambil audit data → sistem membuat header CSV → setiap produk diekspor bersama statistik detail transaksi → file dikirim sebagai attachment.                                                          |
| Hak Akses          | Hanya **admin**. Controller dan `AuthFilter` sama-sama membatasi area ini.                                                                                                                                                                             |
| Kondisi & Batasan  | Export ini adalah **export audit/maintenance**, bukan export laporan penjualan. Statistik `jumlah_detail` berasal dari seluruh baris `detail_transaksi`, termasuk yang terkait transaksi batal.                                                        |
| Data yang Terlibat | `produk`, `kategori`, `detail_transaksi`, `transaksi`; CSV berisi ID, nama, kategori, satuan, harga, status aktif/locked, jumlah detail, total quantity, tanggal transaksi pertama/terakhir, dan Aksi.                                                 |
| Catatan            | Encoding UTF-8 BOM ditulis ke output CSV untuk kompatibilitas dengan spreadsheet. Nama file menggunakan timestamp server.                                                                                                                              |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                                                           |

| Nama Subbab        | Pencarian Produk                                                                                                                                                                                                                                     |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Memungkinkan admin menemukan produk pada master dengan cepat berdasarkan atribut produk yang relevan.                                                                                                                                                |
| Deskripsi          | Pada daftar master, pencarian menggunakan DataTables server-side melalui `/produk/get-produk-data`. Keyword diteruskan ke `ProdukModel::getDataTablesProduk()` dan dicari pada nama produk, barcode, kategori, atau satuan.                          |
| Aturan Bisnis      | Pencarian master hanya bekerja terhadap produk aktif karena query DataTables memiliki kondisi `produk.is_active = 1`. Hasil dapat dipadukan dengan pagination dan sorting DataTables.                                                                |
| Alur               | Admin membuka daftar Produk → memasukkan keyword → DataTables mengirim parameter search → controller meneruskan parameter ke model → model melakukan `LIKE` pada nama/barcode/kategori/satuan → hasil filtered dikembalikan dalam format DataTables. |
| Hak Akses          | Hanya **admin**, karena endpoint `/produk/get-produk-data` termasuk area Produk yang dilindungi `AuthFilter`.                                                                                                                                        |
| Kondisi & Batasan  | Produk nonaktif tidak ikut hasil pencarian master. Pencarian ini berbeda dengan pencarian produk pada POS: POS memuat katalog produk aktif dan melakukan filtering/pencarian di browser.                                                             |
| Data yang Terlibat | `produk.nama`, `produk.barcode`, `produk.satuan`, `produk.is_active`, serta `kategori.nama`.                                                                                                                                                         |
| Catatan            | Model mengembalikan `recordsTotal`, `recordsFiltered`, dan data sesuai protokol DataTables. Default ordering ketika parameter order tidak diberikan adalah `produk.id DESC`.                                                                         |
| Status             | Terimplementasi pada `v2.1`.                                                                                                                                                                                                                         |
