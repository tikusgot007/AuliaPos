| Nama Subbab        | Dashboard Laporan                                                                                                                                                                                                                                                                                              |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menjadi halaman utama untuk melihat dan menjalankan berbagai jenis laporan penjualan/pemasukan berdasarkan tanggal, periode, bulan, dan kategori.                                                                                                                                                              |
| Deskripsi          | `/laporan` dirender oleh `Laporan::index()` dan menyediakan tab **Harian, Bulanan, Periode, dan Per Kategori**. Data diambil AJAX melalui `POST /laporan/get-data`. Dashboard Harian juga menyediakan KPI dan grafik distribusi kategori serta metode pembayaran.                                              |
| Aturan Bisnis      | Laporan tidak boleh mencampur status transaksi dan status pembayaran. Transaksi `batal` dikecualikan dari laporan. Transaksi `mangkrak` tetap termasuk laporan selama memenuhi query laporan yang menggunakan `status != batal`. Data transaksi lama yang sudah di-archive tetap harus dapat masuk ke laporan. |
| Alur               | Buka `/laporan` → pilih tab dan filter → frontend mengirim `jenis`, `tanggal_awal`, `tanggal_akhir`, dan `kategori_id` → `Laporan::getData()` memilih jalur laporan → data live dan archive digabung bila diperlukan → backend melakukan agregasi → hasil JSON dirender ke tabel/KPI/grafik.                   |
| Hak Akses          | **Admin only.** `Laporan::index()` sendiri mengecek `session role=admin`, dan prefix `laporan` termasuk area admin pada `AuthFilter`.                                                                                                                                                                          |
| Kondisi & Batasan  | Laporan bergantung pada tanggal bisnis masing-masing data. Laporan pemasukan menggunakan `pembayaran.tanggal`, bukan `created_at`. Jalur Harian dan Bulanan memiliki logic khusus yang terpisah dari jalur laporan lama Periode/Per Kategori.                                                                  |
| Data yang Terlibat | `transaksi`, `detail_transaksi`, `pembayaran`, `kategori`, `pelanggan`, `users`, database archive; `Laporan`, `TransaksiArchiveService`, dan model transaksi/detail.                                                                                                                                           |
| Catatan            | `docs/AULIA.md` menetapkan Laporan sebagai **dual-source MySQL + archive**. Archive dibaca terpisah karena database utama dan SQLite archive tidak dapat melakukan JOIN lintas database; hasil kemudian digabung di PHP sebelum diproses.                                                                      |
| Status             | Aktif di v2.1.                                                                                                                                                                                                                                                                                                 |

| Nama Subbab        | Laporan Transaksi                                                                                                                                                                                                                                                                        |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menampilkan rekap transaksi dalam rentang tanggal yang dipilih untuk melihat aktivitas penjualan dan nilai transaksi.                                                                                                                                                                    |
| Deskripsi          | Jalur laporan transaksi menggunakan `Laporan::getData()` dan logic `processData()`, `processDetailTransaksi()`, serta `calculateSummary()`. Data transaksi diambil dari `transaksi` beserta pelanggan dan detail produk, kemudian transaksi archive ditambahkan sebelum proses agregasi. |
| Aturan Bisnis      | Transaksi dengan `status=batal` tidak dihitung. Status `proses` dan `selesai` dapat tetap masuk selama bukan `batal`. `mangkrak` tidak otomatis dikeluarkan dari laporan. Perhitungan harus tetap membedakan status pekerjaan transaksi dari status pembayaran.                          |
| Alur               | Pilih tab Periode → tentukan tanggal awal/akhir → request `/laporan/get-data` dengan `jenis=periode` → query transaksi aktif/non-batal → ambil detail → ambil transaksi/detail archive → gabungkan → proses per transaksi/detail → hitung summary → tampilkan hasil.                     |
| Hak Akses          | Admin only.                                                                                                                                                                                                                                                                              |
| Kondisi & Batasan  | Rentang tanggal dapat melewati periode yang sudah di-archive. Data archive harus digabung dengan data MySQL agar histori tidak hilang dari laporan.                                                                                                                                      |
| Data yang Terlibat | `transaksi`, `detail_transaksi`, `pelanggan`, `kategori`, archive `transaksi_archive` dan `detail_transaksi_archive`.                                                                                                                                                                    |
| Catatan            | Source laporan menggunakan data transaksi sebagai dasar, sedangkan nilai pemasukan harian memiliki jalur perhitungan tersendiri berbasis pembayaran. Karena itu “Laporan Transaksi” tidak boleh disamakan dengan “Laporan Pembayaran”.                                                   |
| Status             | Aktif di v2.1.                                                                                                                                                                                                                                                                           |

| Nama Subbab        | Laporan Item Harian                                                                                                                                                                                                                                                                                                              |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menampilkan item yang terjual berdasarkan tanggal pembayaran sehingga penjualan dapat dianalisis sampai level produk/kategori.                                                                                                                                                                                                   |
| Deskripsi          | Endpoint `/laporan/item-harian` menjalankan `Laporan::itemHarian()`. Data berasal dari pembayaran aktif dan detail transaksi, dengan agregasi berdasarkan tanggal pembayaran, transaksi, kategori, dan nama produk. Archive juga dilengkapi melalui `TransaksiArchiveService::getItemHarianMentah()`.                            |
| Aturan Bisnis      | Pembayaran harus `aktif` dan transaksi `batal` dikecualikan. Nilai pembayaran dapat dialokasikan secara proporsional ke detail berdasarkan subtotal detail terhadap total subtotal transaksi. Filter dapat mencakup tanggal, kategori, keyword produk, dan metode pembayaran sesuai parameter yang digunakan controller/service. |
| Alur               | Buka `/laporan/item-harian` → tentukan filter → query pembayaran + transaksi + detail → agregasi item → query sumber archive untuk periode sama → gabungkan hasil live/archive → tampilkan rekap item harian.                                                                                                                    |
| Hak Akses          | **Route saat ini hanya menggunakan `Laporan::itemHarian` tanpa deklarasi `filter => auth` pada `Routes.php`.** Ini berbeda dengan `/laporan` dan `/laporan-pembayaran` yang memakai auth.                                                                                                                                        |
| Kondisi & Batasan  | Karena laporan berbasis `pembayaran.tanggal`, pembayaran backdate akan masuk ke tanggal pembayaran sebenarnya. Transaksi archive diproses melalui query archive terpisah karena tidak dapat JOIN langsung ke MySQL.                                                                                                              |
| Data yang Terlibat | `pembayaran`, `transaksi`, `detail_transaksi`, `kategori`, `produk`/nama produk, serta tabel archive yang bersesuaian.                                                                                                                                                                                                           |
| Catatan            | `TransaksiArchiveService::getItemHarianMentah()` sengaja melakukan agregasi di SQLite terlebih dahulu agar hasil archive dapat langsung digabung dengan hasil live tanpa JOIN lintas database.                                                                                                                                   |
| Status             | Aktif di v2.1; route item harian memiliki perbedaan enforcement autentikasi yang perlu diperhatikan.                                                                                                                                                                                                                             |

| Nama Subbab        | Laporan Pembayaran                                                                                                                                                                                                                                                                 |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menampilkan histori pembayaran aktual berdasarkan tanggal penerimaan, metode pembayaran, dan transaksi yang terkait.                                                                                                                                                               |
| Deskripsi          | `/laporan-pembayaran` menggunakan `Laporan::pembayaran()`. Data pembayaran live digabung dengan hasil `TransaksiArchiveService::getDaftarPembayaranMentah()` untuk mencakup histori yang sudah diarsipkan.                                                                         |
| Aturan Bisnis      | Pembayaran yang dihitung adalah pembayaran efektif/aktif. Pembayaran yang sudah `reversed` tidak dianggap sebagai penerimaan efektif. Transaksi `batal` dikecualikan. Tanggal laporan mengikuti `pembayaran.tanggal`, yaitu tanggal uang benar-benar diterima, bukan `created_at`. |
| Alur               | Buka `/laporan-pembayaran` → pilih rentang tanggal dan filter yang tersedia → controller query pembayaran live → query pembayaran archive untuk rentang yang sama → gabungkan → urutkan/proses → tampilkan daftar pembayaran.                                                      |
| Hak Akses          | User login melalui route `auth`; tidak terdapat prefix admin-only khusus pada route `/laporan-pembayaran`.                                                                                                                                                                         |
| Kondisi & Batasan  | Pembayaran backdate dapat mengubah distribusi pembayaran antar tanggal laporan karena `tanggal` adalah tanggal penerimaan sebenarnya. Koreksi pembayaran mempertahankan record lama sebagai histori `reversed`, tetapi hanya pembayaran aktif yang menjadi nilai efektif.          |
| Data yang Terlibat | `pembayaran`, `transaksi`, `pelanggan`, `users`, `pembayaran_archive`, `transaksi_archive`.                                                                                                                                                                                        |
| Catatan            | `TransaksiArchiveService::getDaftarPembayaranMentah()` dirancang untuk mengembalikan bentuk data yang kompatibel dengan hasil query live agar dapat langsung digabung oleh `Laporan::pembayaran()`.                                                                                |
| Status             | Aktif di v2.1.                                                                                                                                                                                                                                                                     |

| Nama Subbab        | Export Laporan                                                                                                                                                                                                                                                                                    |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Mengeluarkan data laporan ke format yang dapat diproses atau dibuka di spreadsheet.                                                                                                                                                                                                               |
| Deskripsi          | `POST /laporan/export-excel` ditangani `Laporan::exportExcel()`. Meskipun namanya `exportExcel`, source controller mendokumentasikan mekanismenya sebagai **export CSV tanpa PhpSpreadsheet**. Parameter laporan dibaca dari POST seperti `jenis`, `tanggal_awal`, dan parameter terkait laporan. |
| Aturan Bisnis      | Export mengikuti periode/jenis laporan yang diminta dan menggunakan data laporan, bukan dataset terpisah yang mempunyai aturan bisnis sendiri. Transaksi `batal` tetap tidak boleh masuk ke hasil laporan yang memang menggunakan aturan laporan non-batal.                                       |
| Alur               | User memilih laporan → request export dikirim ke `/laporan/export-excel` → controller membaca parameter filter → mengambil/mengolah data → menghasilkan file CSV untuk diunduh.                                                                                                                   |
| Hak Akses          | Admin only melalui area laporan/AuthFilter.                                                                                                                                                                                                                                                       |
| Kondisi & Batasan  | Nama endpoint tetap `export-excel`, tetapi implementasi source menggunakan CSV sebagai alternatif tanpa dependency PhpSpreadsheet.                                                                                                                                                                |
| Data yang Terlibat | Dataset hasil laporan: `transaksi`, `detail_transaksi`, `pembayaran`, `kategori`, pelanggan, dan sumber archive bila jalur export menggunakannya.                                                                                                                                                 |
| Catatan            | Export harus dipahami sebagai representasi data laporan, bukan sebagai mekanisme perubahan data.                                                                                                                                                                                                  |
| Status             | Aktif di v2.1.                                                                                                                                                                                                                                                                                    |

| Nama Subbab        | Filter Laporan                                                                                                                                                                                                                       |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Membatasi data laporan berdasarkan periode, tanggal, kategori, dan jenis laporan yang dipilih.                                                                                                                                       |
| Deskripsi          | Dashboard menyediakan filter berbeda untuk setiap tab: Harian menggunakan satu tanggal, Bulanan menggunakan bulan+tahun, Periode menggunakan rentang tanggal, dan Per Kategori menggunakan rentang tanggal plus kategori.            |
| Aturan Bisnis      | Filter tanggal diterjemahkan ke `tanggal_awal` dan `tanggal_akhir` sebelum dikirim ke backend. Filter kategori hanya diterapkan pada jalur Per Kategori. Laporan Harian menggunakan tanggal pembayaran untuk jalur pemasukan harian. |
| Alur               | User memilih filter → JavaScript mengubah tanggal UI menjadi `YYYY-MM-DD` → request JSON dikirim ke `/laporan/get-data` → controller memilih jalur sesuai `jenis` → backend menerapkan filter → hasil dikembalikan sebagai JSON.     |
| Hak Akses          | Dashboard laporan admin-only. Endpoint `/laporan/get-data` sendiri memakai `auth`, sedangkan akses bisnis ke halaman laporan dibatasi controller/AuthFilter.                                                                         |
| Kondisi & Batasan  | Jika filter Periode/Kategori kosong, frontend menggunakan awal bulan sampai hari ini. Harian default ke hari ini. Bulanan default ke bulan/tahun berjalan.                                                                           |
| Data yang Terlibat | Parameter `jenis`, `tanggal_awal`, `tanggal_akhir`, `kategori_id`; tabel transaksi, pembayaran, detail transaksi, kategori, dan archive.                                                                                             |
| Catatan            | Filter yang tampil di UI tidak selalu berarti filter SQL langsung. Contohnya beberapa jalur laporan melakukan query sumber terlebih dahulu kemudian agregasi/filter di PHP.                                                          |
| Status             | Aktif di v2.1.                                                                                                                                                                                                                       |

| Nama Subbab        | Pencarian Laporan                                                                                                                                                                                                 |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menemukan atau membatasi data laporan berdasarkan atribut yang relevan dengan laporan item/transaksi.                                                                                                             |
| Deskripsi          | Pada laporan item harian, source mendukung parameter `keyword` untuk mencocokkan nama produk. Pada laporan utama, mekanisme utama bukan search box global, melainkan filter tanggal, jenis laporan, dan kategori. |
| Aturan Bisnis      | Pencarian item menggunakan nama produk dan dapat dikombinasikan dengan kategori serta metode pembayaran. Pencarian tidak boleh memasukkan transaksi `batal`.                                                      |
| Alur               | User memasukkan keyword/filter → parameter dikirim ke endpoint laporan yang sesuai → query live dan archive menggunakan kriteria yang sama → hasil agregasi ditampilkan.                                          |
| Hak Akses          | Mengikuti hak akses endpoint laporan yang digunakan.                                                                                                                                                              |
| Kondisi & Batasan  | Tidak terdapat satu fitur “global search laporan” yang bekerja terhadap seluruh jenis laporan. `Api::searchGlobal()` berkaitan dengan pencarian transaksi dan bukan mekanisme pencarian utama dashboard laporan.  |
| Data yang Terlibat | `detail_transaksi.nama_produk`, `kategori_id`, `pembayaran.metode`, `pembayaran.tanggal`, transaksi live/archive.                                                                                                 |
| Catatan            | `TransaksiArchiveService::getItemHarianMentah()` juga menerapkan `keyword` terhadap `dt.nama_produk`, sehingga pencarian item dapat mencakup data archive.                                                        |
| Status             | Aktif di v2.1.                                                                                                                                                                                                    |

| Nama Subbab        | Riwayat Laporan                                                                                                                                                                                                                                        |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menyediakan akses terhadap data laporan historis, termasuk transaksi dan pembayaran yang telah dipindahkan ke archive.                                                                                                                                 |
| Deskripsi          | Tidak terdapat tabel atau halaman `riwayat laporan` khusus. Histori laporan direalisasikan melalui filter periode dan mekanisme **dual-source** pada `Laporan.php`: MySQL aktif dan SQLite archive.                                                    |
| Aturan Bisnis      | Data lama yang sudah di-archive tetap harus dapat muncul pada laporan. Archive tidak mengubah status atau nilai transaksi saat dipindahkan. Transaksi `batal` tetap mengikuti aturan pengecualian laporan.                                             |
| Alur               | User memilih periode historis → laporan query MySQL → `TransaksiArchiveService` query SQLite archive untuk periode yang sama → data digabung di PHP → logic agregasi laporan yang sama digunakan untuk hasil akhir.                                    |
| Hak Akses          | Mengikuti hak akses masing-masing endpoint laporan. Dashboard `/laporan` dibatasi admin; `/laporan-pembayaran` menggunakan auth; route item harian pada v2.1 tidak mendeklarasikan auth filter.                                                        |
| Kondisi & Batasan  | Archive hanya berisi transaksi yang telah dipindahkan oleh proses Archive Transaksi. Data archive bersifat read-only dalam konteks laporan. MySQL tidak dapat melakukan JOIN langsung dengan SQLite sehingga integrasi dilakukan di application layer. |
| Data yang Terlibat | MySQL `transaksi`, `detail_transaksi`, `pembayaran`; SQLite `transaksi_archive`, `detail_transaksi_archive`, `pembayaran_archive`; `TransaksiArchiveService`.                                                                                          |
| Catatan            | `docs/AULIA.md` menetapkan Laporan sebagai **dual-source penuh**. Ini berbeda dengan Tagihan, yang tidak melakukan fallback archive pada `Tagihan::detail()`.                                                                                          |
| Status             | Aktif di v2.1.                                                                                                                                                                                                                                         |
