| Nama Subbab        | Daftar Karyawan                                                                                                                                                                                                                                                                             |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menyediakan daftar karyawan yang menjadi referensi pengelolaan jadwal dan roster.                                                                                                                                                                                                           |
| Deskripsi          | Pada pengelolaan jadwal, employee picker untuk membuat jadwal baru hanya mengambil karyawan `is_active=1`. Untuk tampilan Matrix/Calendar periode tertentu, sistem menampilkan seluruh karyawan aktif ditambah karyawan nonaktif yang masih mempunyai histori jadwal pada periode tersebut. |
| Aturan Bisnis      | Karyawan nonaktif tidak dapat dijadikan schedule baru apabila belum mempunyai histori. Karyawan nonaktif yang sudah memiliki histori tetap dapat ditampilkan dan jadwal historisnya dapat dikoreksi.                                                                                        |
| Alur               | Controller mengambil data karyawan dari `UserModel`/`JadwalModel` → filter status aktif sesuai konteks → urut berdasarkan nama → digunakan oleh Matrix, Calendar, Master, atau employee picker.                                                                                             |
| Hak Akses          | Pengelolaan jadwal/mutation hanya Admin. Data roster read-only dapat dilihat seluruh role yang login.                                                                                                                                                                                       |
| Kondisi & Batasan  | Tidak ada CRUD karyawan baru di modul Jadwal; sumber data karyawan adalah tabel `users`. Nama, inisial, divisi, dan status aktif berasal dari data user.                                                                                                                                    |
| Data yang Terlibat | `users.id`, `users.nama`, `users.inisial`, `users.divisi`, `users.is_active`, serta relasi `jadwal.karyawan_id`.                                                                                                                                                                            |
| Catatan            | Tidak ada konsep status hadir/absen di sini. Tidak adanya row jadwal berarti **belum dijadwalkan**, bukan otomatis libur.                                                                                                                                                                   |
| Status             | Aktif di v2.1                                                                                                                                                                                                                                                                               |

| Nama Subbab        | Jadwal Karyawan                                                                                                                                                                       |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Mengelola actual schedule karyawan berdasarkan tanggal dan shift kerja.                                                                                                               |
| Deskripsi          | Modul utama `/jadwal` mengelola jadwal aktual pada tabel `jadwal`. Satu karyawan hanya memiliki maksimal satu row per tanggal. Shift yang valid adalah `P`, `S`, `PM`, dan `L`.       |
| Aturan Bisnis      | `P` = 08:00–15:00, `S` = 13:30–20:30, `PM` = 08:00–12:30 dan 18:00–20:30, `L` = Libur. Tidak ada penyimpanan jam kerja per row karena jam berasal dari `JadwalModel::DEFINISI_SHIFT`. |
| Alur               | Admin membuka `/jadwal` → memilih Matrix/Calendar/Master → membaca atau mengubah schedule → perubahan actual disimpan ke `jadwal`.                                                    |
| Hak Akses          | Mutation jadwal Admin-only. Roster read-only menggunakan prefix `/roster` dan tersedia untuk seluruh role login.                                                                      |
| Kondisi & Batasan  | Jadwal bukan absensi. Login, online status, check-in, atau check-out tidak mengubah status jadwal. Jadwal juga tidak digunakan untuk memblokir transaksi.                             |
| Data yang Terlibat | `jadwal.id`, `karyawan_id`, `tanggal`, `shift`, `created_at`, `updated_at`, serta data karyawan dari `users`.                                                                         |
| Catatan            | PM hanya satu row schedule walaupun mempunyai dua sesi kerja.                                                                                                                         |
| Status             | Aktif di v2.1                                                                                                                                                                         |

| Nama Subbab        | Matrix Jadwal                                                                                                                                                                                                |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menampilkan jadwal mingguan dalam bentuk matriks karyawan × Senin–Minggu untuk pengelolaan operasional.                                                                                                      |
| Deskripsi          | `/jadwal` membangun Matrix berdasarkan minggu yang dipilih. Data schedule dipetakan menjadi `[karyawan_id][tanggal]` dan ID row juga dipetakan agar aksi edit/hapus/swap dapat langsung mengacu ke schedule. |
| Aturan Bisnis      | Minggu dimulai Senin dan berakhir Minggu. Karyawan aktif ditampilkan bersama karyawan nonaktif yang memiliki histori pada periode tersebut.                                                                  |
| Alur               | Tentukan minggu → ambil karyawan relevan → ambil schedule periode 7 hari → bentuk `peta` shift dan `peta_id` → render Matrix. Navigasi minggu dapat menggunakan `/jadwal/matrix-data`.                       |
| Hak Akses          | Admin untuk Matrix pengelolaan. Versi read-only untuk semua role tersedia melalui `/roster`.                                                                                                                 |
| Kondisi & Batasan  | Filter dapat berupa divisi, shift, dan pencarian nama/inisial. Ringkasan availability tetap menghitung seluruh divisi target dan tidak mengikuti filter shift/search Matrix.                                 |
| Data yang Terlibat | `users`, `jadwal`, `shift`, `tanggal`, `divisi`, `inisial`, dan struktur `availability`.                                                                                                                     |
| Catatan            | Availability hanya menghitung karyawan aktif dengan shift `P/S/PM` pada divisi `Wanita`, `Pria`, dan `Banner`; `L` dan belum dijadwalkan tidak dihitung sebagai availability.                                |
| Status             | Aktif di v2.1                                                                                                                                                                                                |

| Nama Subbab        | Kalender Jadwal                                                                                                                                                                |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menampilkan actual schedule dalam format kalender berbasis tanggal dan jam kerja.                                                                                              |
| Deskripsi          | Endpoint `/jadwal/calendar-events` menghasilkan event untuk FullCalendar. Shift kerja menggunakan jam dari `JadwalModel::DEFINISI_SHIFT`, sedangkan `L` menjadi event all-day. |
| Aturan Bisnis      | `P`, `S`, dan `PM` menggunakan sesi jam masing-masing. `PM` menghasilkan dua event visual tetapi membawa `schedule_id` yang sama karena secara bisnis tetap satu row schedule. |
| Alur               | FullCalendar mengirim rentang `start/end` → controller mengambil schedule periode → setiap shift diterjemahkan ke sesi jam → hasil dikirim sebagai JSON event.                 |
| Hak Akses          | Admin untuk kalender pengelolaan. Roster memiliki tampilan mingguan/bulanan read-only untuk semua role login.                                                                  |
| Kondisi & Batasan  | Calendar tidak menyimpan jam ke database. Event `L` tidak memiliki jam spesifik.                                                                                               |
| Data yang Terlibat | `jadwal.id`, `karyawan_id`, `tanggal`, `shift`, nama/inisial/divisi karyawan, dan `DEFINISI_SHIFT`.                                                                            |
| Catatan            | Untuk PM, dua event visual bukan dua schedule berbeda; edit/hapus harus memperlakukan keduanya sebagai satu schedule.                                                          |
| Status             | Aktif di v2.1                                                                                                                                                                  |

| Nama Subbab        | Master Jadwal                                                                                                                                                                                                        |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menyediakan template jadwal mingguan yang dapat digunakan berulang untuk membuat actual schedule.                                                                                                                    |
| Deskripsi          | `master_jadwal` merupakan template yang independen dari tabel `jadwal`. Pada implementasi saat ini terdapat satu master aktif yang digunakan UI; jika belum ada, controller membuat `Master Jadwal` secara otomatis. |
| Aturan Bisnis      | Cell master menyimpan karyawan × hari (`1–7`) × shift. Cell yang tidak mempunyai row berarti tidak ada assignment, bukan `L`. Master tidak otomatis mengubah actual schedule setelah pernah di-apply.                |
| Alur               | Buka master → sistem mengambil header dan detail → Admin mengubah cell → data disimpan ke `master_jadwal_detail` → master dapat diterapkan ke minggu tertentu melalui Apply.                                         |
| Hak Akses          | Admin-only.                                                                                                                                                                                                          |
| Kondisi & Batasan  | Shift harus `P/S/PM/L`; hari hanya 1–7. Perubahan master setelah Apply tidak retroaktif terhadap actual schedule.                                                                                                    |
| Data yang Terlibat | `master_jadwal`, `master_jadwal_detail`, `karyawan_id`, `hari`, `shift`, dan data `users`.                                                                                                                           |
| Catatan            | Master adalah template, bukan sumber kebenaran actual schedule.                                                                                                                                                      |
| Status             | Aktif di v2.1                                                                                                                                                                                                        |
                                                                                                                                                     |

| Nama Subbab        | Tambah Jadwal                                                                                                                                                                                                    |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Membuat schedule actual untuk satu atau beberapa karyawan pada tanggal dan shift tertentu.                                                                                                                       |
| Deskripsi          | Endpoint `/jadwal/simpan` menerima `tanggal`, `shift`, dan satu atau beberapa `karyawan_id`. Backend melakukan validasi ulang sebelum menyimpan.                                                                 |
| Aturan Bisnis      | Tanggal, shift, dan minimal satu karyawan wajib ada. Shift hanya `P/S/PM/L`. Karyawan baru untuk schedule harus aktif.                                                                                           |
| Alur               | Admin memilih tanggal → memilih shift → memilih karyawan → request dikirim → backend validasi user/shift → `simpanJadwal()` melakukan INSERT jika belum ada row atau UPDATE jika row tanggal tersebut sudah ada. |
| Hak Akses          | Admin-only.                                                                                                                                                                                                      |
| Kondisi & Batasan  | Karyawan nonaktif yang belum mempunyai histori ditolak. Karyawan nonaktif yang sudah mempunyai histori masih dapat diproses untuk koreksi data lama. Tidak ada pembatasan divisi saat create/edit manual.        |
| Data yang Terlibat | `users.id/is_active`, `jadwal.karyawan_id`, `jadwal.tanggal`, `jadwal.shift`.                                                                                                                                    |
| Catatan            | Unique `karyawan_id + tanggal` mencegah duplicate schedule.                                                                                                                                                      |
| Status             | Aktif di v2.1                                                                                                                                                                                                    |

| Nama Subbab        | Edit Jadwal                                                                                                                                             |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Mengubah shift dari schedule actual yang sudah ada.                                                                                                     |
| Deskripsi          | Edit menggunakan mekanisme `/jadwal/simpan`; jika row `karyawan_id + tanggal` sudah ada, `simpanJadwal()` melakukan UPDATE terhadap shift row tersebut. |
| Aturan Bisnis      | Shift harus valid. Tidak ada perubahan otomatis terhadap master jadwal.                                                                                 |
| Alur               | Admin memilih cell → mengubah shift → backend mencari row berdasarkan karyawan dan tanggal → update shift → schedule aktual berubah.                    |
| Hak Akses          | Admin-only.                                                                                                                                             |
| Kondisi & Batasan  | Edit histori karyawan nonaktif tetap dimungkinkan apabila row schedule memang sudah ada. Divisi tidak menjadi pembatas edit manual.                     |
| Data yang Terlibat | `jadwal.id`, `karyawan_id`, `tanggal`, `shift`, `updated_at`.                                                                                           |
| Catatan            | Mengubah actual schedule tidak mengubah `master_jadwal`.                                                                                                |
| Status             | Aktif di v2.1                                                                                                                                           |

| Nama Subbab        | Hapus Jadwal                                                                                                                            |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menghapus actual schedule yang tidak lagi diperlukan atau melakukan koreksi roster.                                                     |
| Deskripsi          | Hapus satu schedule menggunakan `/jadwal/hapus/{id}`. Tersedia juga penghapusan range melalui `/jadwal/hapus-range`.                    |
| Aturan Bisnis      | Penghapusan hanya menyentuh tabel actual `jadwal`; master tidak ikut dihapus. Hapus range secara eksplisit hanya dapat dilakukan Admin. |
| Alur               | Admin memilih schedule/range → backend memanggil `hapusJadwal()` atau `hapusRange()` → row actual dihapus.                              |
| Hak Akses          | Admin-only. `hapusRange()` juga melakukan pengecekan role Admin secara eksplisit sebagai defense-in-depth.                              |
| Kondisi & Batasan  | Menghapus row berarti status menjadi **belum dijadwalkan**, bukan otomatis `L`.                                                         |
| Data yang Terlibat | `jadwal.id`, `karyawan_id`, `tanggal`, `shift`.                                                                                         |
| Catatan            | Tidak ada operasi penghapusan terhadap `master_jadwal` dalam fungsi hapus actual.                                                       |
| Status             | Aktif di v2.1                                                                                                                           |

| Nama Subbab        | Swap Jadwal                                                                                                                                                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menukar penugasan shift antar dua schedule tanpa memindahkan kepemilikan row database.                                                                                                                                          |
| Deskripsi          | `/jadwal/swap` menerima dua `schedule_id`. Implementasi saat ini menukar **nilai shift**, bukan `karyawan_id` atau `tanggal`, sehingga unique constraint tidak berisiko bentrok.                                                |
| Aturan Bisnis      | Kedua karyawan wajib berada pada divisi yang sama. Jika shift sama, swap ditolak. PM diperlakukan sebagai satu nilai shift utuh. Jika kedua cell sama-sama `L` dan karyawannya berbeda, digunakan mekanisme Tukar Libur khusus. |
| Alur               | Pilih dua schedule → backend mengambil kedua row → validasi karyawan/divisi/shift → tukar nilai shift dalam transaction → commit.                                                                                               |
| Hak Akses          | Admin-only.                                                                                                                                                                                                                     |
| Kondisi & Batasan  | Swap lintas tanggal diperbolehkan karena row tidak berpindah. Tukar Libur membutuhkan kedua karyawan mempunyai schedule kerja pada tanggal lawan; jika salah satu tidak ada atau juga `L`, operasi ditolak.                     |
| Data yang Terlibat | Dua row `jadwal`, `karyawan_id`, `tanggal`, `shift`, dan `users.divisi`.                                                                                                                                                        |
| Catatan            | Desain swap ini merupakan revisi v2.1 untuk menghindari bentrok `unique(karyawan_id,tanggal)` yang dapat terjadi jika kepemilikan row ditukar.                                                                                  |
| Status             | Aktif di v2.1                                                                                                                                                                                                                   |

| Nama Subbab        | Apply Master Jadwal                                                                                                                                                                                                    |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menerapkan template Master Jadwal ke actual schedule untuk sejumlah minggu.                                                                                                                                            |
| Deskripsi          | `/jadwal/master/apply` menerima master, tanggal mulai, jumlah minggu, dan flag `overwrite`. Tanggal mulai wajib Senin.                                                                                                 |
| Aturan Bisnis      | Default hanya mengisi slot actual yang kosong. Jika slot sudah ada dengan shift berbeda, slot menjadi conflict dan dilewati. `overwrite=true` hanya digunakan melalui aksi eksplisit untuk menimpa schedule yang ada.  |
| Alur               | Admin pilih master → pilih Senin awal → tentukan jumlah minggu → pilih mode overwrite atau default → backend memproses setiap karyawan/hari → menghasilkan jumlah `diisi`, `dilewati`, `ditimpa`, dan daftar conflict. |
| Hak Akses          | Admin-only.                                                                                                                                                                                                            |
| Kondisi & Batasan  | Master kosong tidak menghasilkan perubahan. Slot yang sudah mempunyai shift sama tidak dianggap conflict dan tidak perlu ditulis ulang.                                                                                |
| Data yang Terlibat | `master_jadwal`, `master_jadwal_detail`, `jadwal`, `karyawan_id`, tanggal, shift.                                                                                                                                      |
| Catatan            | Apply menghasilkan actual schedule; setelah itu master dan actual schedule tetap independen.                                                                                                                           |
| Status             | Aktif di v2.1                                                                                                                                                                                                          |

| Nama Subbab        | Roster Karyawan                                                                                                                                                             |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Memberikan tampilan jadwal read-only kepada seluruh karyawan tanpa membuka kemampuan mutation jadwal Admin.                                                                 |
| Deskripsi          | `/roster` menggunakan data `JadwalModel` yang sama dengan Matrix Admin, tetapi tidak mengirim `peta_id` dan tidak memiliki aksi edit/hapus/swap.                            |
| Aturan Bisnis      | Roster hanya membaca actual schedule. Roster bukan attendance dan tidak mempunyai kewenangan mengubah jadwal.                                                               |
| Alur               | User login → buka `/roster` → sistem mengambil jadwal periode → tampilkan roster mingguan dan ringkasan hari ini → data dapat dinavigasi/filter melalui endpoint read-only. |
| Hak Akses          | Semua role login, termasuk `admin` dan `kasir`.                                                                                                                             |
| Kondisi & Batasan  | Prefix `/roster` sengaja dipisahkan dari `/jadwal` agar tidak terkena admin route gate.                                                                                     |
| Data yang Terlibat | `users`, `jadwal`, tanggal, shift, divisi, inisial, dan user session.                                                                                                       |
| Catatan            | Roster tidak mempunyai mekanisme mutation meskipun data yang ditampilkan sama dengan data yang dikelola Admin.                                                              |
| Status             | Aktif di v2.1                                                                                                                                                               |

| Nama Subbab        | Roster Bulanan                                                                                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Melihat jadwal karyawan dalam cakupan satu bulan penuh secara read-only.                                                                                           |
| Deskripsi          | `/roster/bulan-data` menerima parameter `YYYY-MM`, kemudian menentukan tanggal awal dan akhir bulan serta jumlah hari bulan tersebut.                              |
| Aturan Bisnis      | Data tetap berasal dari actual `jadwal`, bukan master. Filter dapat berupa divisi, shift, dan pencarian nama/inisial.                                              |
| Alur               | User memilih bulan → backend menghitung range bulan → mengambil karyawan dan schedule → membentuk `peta[karyawan_id][tanggal]` → JSON digunakan UI roster bulanan. |
| Hak Akses          | Semua role login.                                                                                                                                                  |
| Kondisi & Batasan  | Read-only; tidak ada `schedule_id` untuk aksi edit. Karyawan relevan dapat mencakup karyawan nonaktif yang mempunyai histori pada periode tersebut.                |
| Data yang Terlibat | `users`, `jadwal`, `tanggal`, `shift`, `divisi`, `inisial`.                                                                                                        |
| Catatan            | Jumlah hari mengikuti kalender aktual bulan yang dipilih.                                                                                                          |
| Status             | Aktif di v2.1                                                                                                                                                      |

| Nama Subbab        | Roster Hari Ini                                                                                                                                                         |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menampilkan kondisi roster pada tanggal tertentu dan siapa saja yang masuk setiap shift.                                                                                |
| Deskripsi          | `/roster/status-hari-ini`/endpoint terkait membentuk kelompok `Pagi`, `Siang`, `PM`, `Libur`, serta `Belum Dijadwalkan`. User yang sedang login diberi penanda `saya`.  |
| Aturan Bisnis      | `L` berarti libur eksplisit. Tidak adanya row untuk karyawan aktif berarti **Belum Dijadwalkan**, bukan `L`.                                                            |
| Alur               | Ambil schedule tanggal → kelompokkan berdasarkan shift → ambil seluruh karyawan aktif → karyawan aktif tanpa row dimasukkan ke `belum_dijadwalkan` → tandai user login. |
| Hak Akses          | Semua role login melalui roster read-only.                                                                                                                              |
| Kondisi & Batasan  | Hanya karyawan aktif yang diperiksa untuk kelompok `belum_dijadwalkan`. Status ini bersifat informasi, bukan validasi akses.                                            |
| Data yang Terlibat | `jadwal`, `users`, session `id_user`, `tanggal`, `shift`.                                                                                                               |
| Catatan            | Status hari ini tidak menyimpulkan kehadiran aktual.                                                                                                                    |
| Status             | Aktif di v2.1                                                                                                                                                           |

| Nama Subbab        | Status Jadwal Saya                                                                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Memberikan informasi kepada user mengenai status jadwal dirinya pada waktu saat ini.                                                                   |
| Deskripsi          | `/roster/status-saya` mengambil hanya satu row jadwal user yang sedang login untuk tanggal hari ini, kemudian mengevaluasi status terhadap jam server. |
| Aturan Bisnis      | Status dapat berupa `sesuai`, `belum_masuk`, `lewat`, `jeda`, `libur`, atau `belum_dijadwalkan`. Jam kerja berasal dari `JadwalModel::DEFINISI_SHIFT`. |
| Alur               | Ambil `id_user` dari session → query jadwal hari ini → evaluasi shift terhadap jam server → kembalikan status, pesan, dan tipe toast.                  |
| Hak Akses          | Endpoint roster read-only; ditujukan untuk user yang login.                                                                                            |
| Kondisi & Batasan  | Hanya informasi. Status ini **tidak digunakan untuk authorization**, tidak menentukan boleh/tidaknya transaksi atau aksi lain.                         |
| Data yang Terlibat | Session `id_user`, `jadwal.karyawan_id`, `tanggal`, `shift`, `DEFINISI_SHIFT`, waktu server aplikasi.                                                  |
| Catatan            | Untuk PM, waktu 12:30–18:00 menghasilkan status `jeda`.                                                                                                |
| Status             | Aktif di v2.1                                                                                                                                          |

| Nama Subbab        | Priority Karyawan                                                                                                                                                           |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menentukan urutan prioritas karyawan yang dapat digunakan dalam pemilihan Effective Shift Leader.                                                                           |
| Deskripsi          | Priority merupakan atribut permanen pada `users.priority`, bukan role dan bukan atribut shift. Nilai lebih besar berarti prioritas lebih tinggi.                            |
| Aturan Bisnis      | Priority bersifat unik antar-karyawan. `NULL` berarti belum diberi ranking, bukan Priority 0. Priority tidak pernah mengubah `users.role`; role tetap `admin` atau `kasir`. |
| Alur               | Admin mengatur Priority melalui Manajemen User → nilai disimpan pada `users.priority` → `EffectiveShiftLeaderService` membaca Priority saat menghitung kandidat leader.     |
| Hak Akses          | Hanya Admin yang dapat mengatur Priority. Priority tidak memberikan hak mutation jadwal.                                                                                    |
| Kondisi & Batasan  | Priority saja tidak memberikan authority. Karyawan dengan Priority tinggi tetapi nonaktif atau sedang di luar jam kerja tidak menjadi Effective Shift Leader.               |
| Data yang Terlibat | `users.id`, `users.priority`, `users.role`, `users.is_active`, serta `jadwal`.                                                                                              |
| Catatan            | Nilai Priority wajib unik; pengosongan field pada create/edit menghasilkan `NULL`.                                                                                          |
| Status             | Aktif di v2.1                                                                                                                                                               |

| Nama Subbab        | Shift Member                                                                                                                                                                |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menentukan karyawan yang secara jadwal tercatat sedang bekerja pada hari tersebut.                                                                                          |
| Deskripsi          | Shift Member ditentukan murni dari row `jadwal` hari itu dengan shift `P`, `S`, atau `PM`. `L` tidak termasuk Shift Member.                                                 |
| Aturan Bisnis      | Login, status online, check-in, check-out, atau aktivitas aktual tidak menjadi dasar Shift Member. Karyawan tetap Shift Member walaupun belum login ke AuliaPos.            |
| Alur               | Ambil schedule hari ini → cek shift `P/S/PM` → karyawan dengan row tersebut menjadi Shift Member → status ini kemudian dapat digunakan oleh service Effective Shift Leader. |
| Hak Akses          | Tidak ada assignment manual khusus. Data berasal dari actual schedule yang dikelola Admin.                                                                                  |
| Kondisi & Batasan  | Shift Member bukan bukti kehadiran aktual. Karyawan dengan `L` atau tanpa row tidak termasuk Shift Member.                                                                  |
| Data yang Terlibat | `jadwal.karyawan_id`, `jadwal.tanggal`, `jadwal.shift`, `users.role`, `users.is_active`, `users.priority`.                                                                  |
| Catatan            | Shift Member adalah konsep jadwal, sedangkan Effective Shift Leader adalah hasil evaluasi dinamis terhadap Shift Member + Priority + jam kerja.                             |
| Status             | Aktif di v2.1                                                                                                                                                               |

| Nama Subbab        | Effective Shift Leader                                                                                                                                                                                                                                           |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tujuan             | Menentukan authority operasional sementara berdasarkan karyawan yang sedang bekerja dan memiliki Priority tertinggi.                                                                                                                                             |
| Deskripsi          | Effective Shift Leader dihitung **on-demand**, bukan disimpan sebagai role atau record. Kandidat wajib `role=kasir`, aktif, mempunyai Priority, mempunyai jadwal kerja `P/S/PM`, dan sedang berada dalam jendela jam kerja shift.                                |
| Aturan Bisnis      | Kandidat dari `P`, `S`, dan `PM` masuk satu pool global. Priority tertinggi yang sedang bekerja menjadi Effective Shift Leader. Admin tidak pernah menjadi kandidat. Tidak ada fallback ke leader sebelumnya, shift lain, atau Admin.                            |
| Alur               | Request membutuhkan pengecekan authority → `EffectiveShiftLeaderService` membaca user aktif + Priority + jadwal hari ini → evaluasi jendela waktu → pilih Priority tertinggi → hasil `leader` atau `null`.                                                       |
| Hak Akses          | Status ini dapat memberikan kapabilitas tertentu yang sudah didaftarkan, antara lain menyelesaikan transaksi, membatalkan transaksi, dan melakukan backdate payment sesuai aturan masing-masing fitur. Tidak memberikan hak mutation Jadwal atau Manajemen User. |
| Kondisi & Batasan  | Jika tidak ada kandidat yang sedang berada dalam jam kerjanya, hasilnya `null`. Priority sendiri tidak cukup. Status `Shift Leader` bukan `users.role='shift_leader'`.                                                                                           |
| Data yang Terlibat | `users.id`, `role`, `is_active`, `priority`; `jadwal.karyawan_id`, `tanggal`, `shift`; `JadwalModel::DEFINISI_SHIFT`; `EffectiveShiftLeaderService`; evaluasi jendela kerja.                                                                                     |
| Catatan            | Authority ini terpisah dari `statusJadwalSaya()`. Status jadwal user bersifat informasi dan tidak menentukan otorisasi. Mutation `/jadwal/*` tetap Admin-only.                                                                                                   |
| Status             | Aktif di v2.1                                                                                                                                                                                                                                                    |

| Nama Subbab        | Shift Leader Saat Ini                                                                                                                                                                                                    |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Tujuan             | Menampilkan siapa Effective Shift Leader yang sedang berlaku kepada user yang login.                                                                                                                                     |
| Deskripsi          | Endpoint `GET /roster/shift-leader-saat-ini` memanggil `EffectiveShiftLeaderService::shiftLeaderSaatIni()` dan mengembalikan data leader atau `null`. Endpoint ini bersifat informasi.                                   |
| Aturan Bisnis      | Menggunakan algoritma Effective Shift Leader yang sama: kasir aktif, Priority tidak null, mempunyai shift kerja hari ini, dan sedang berada dalam jendela jam kerja; Priority tertinggi menjadi leader global.           |
| Alur               | Browser melakukan polling endpoint → controller menghitung leader saat itu → response berisi `id`, `username`, `nama`, `inisial`, `priority`, atau `null` → header dapat menampilkan badge leader.                       |
| Hak Akses          | Semua role yang login. Endpoint tidak admin-gated.                                                                                                                                                                       |
| Kondisi & Batasan  | Endpoint tidak menetapkan authority baru dan tidak dipakai sebagai mekanisme authorization. Jika terjadi kegagalan pembacaan DB terkait konsep Priority/jadwal, endpoint melakukan degradasi aman menjadi `leader=null`. |
| Data yang Terlibat | `users`, `jadwal`, `priority`, `EffectiveShiftLeaderService`, session/auth.                                                                                                                                              |
| Catatan            | Header melakukan polling berkala; hasil endpoint hanya informasi. Enforcement authority tetap dilakukan langsung melalui `Authority::isCurrentShiftLeader()` pada fitur yang membutuhkan hak tersebut.                   |
| Status             | Aktif di v2.1                                                                                                                                                                                                            |
                                                                                                                                                                                                                                          |
